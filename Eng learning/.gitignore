# Private assistant memory — never publish
.workbuddy/

# NOTE: As of the 2026-09 combine, the lab is merged into the parent
# dp-study-site repo and the source poem .docx / Thetis.pdf ARE tracked
# on purpose (they are the lab's raw inputs). Only tooling memory + OS
# noise stay ignored.
.DS_Store
