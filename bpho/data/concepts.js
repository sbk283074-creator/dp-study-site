/* BPhO Round 0 — key points.

   Generated by tools/paper2025/build_concepts.py from concepts_a/b/c.py. Edit those,
   never this file. Each entry is a self-contained lesson written for someone meeting
   the idea for the first time: what the quantity is, where the formula comes from,
   what each symbol and unit means, and the trap the paper is setting.

   `stage` orders the course (0 toolkit · 1 mechanics · 2 materials & thermal ·
   3 waves & optics · 4 electricity · 5 quantum & nuclear). `pre` lists what must be
   read first; it is acyclic and never points at a later stage. `q` is the reverse
   index: which of the 25 past-paper questions use this point.

   66 key points, used 77 times across the paper.
*/
window.BPHO_CONCEPTS = [
  {
    id: "units", m: "A", stage: 0, t: "Quantities, base units and derived units",
    pre: [],
    one: "Every physical quantity is a number times a unit, and every unit can be written using seven base units.",
    body: `<p>Physics does not measure bare numbers. It measures <b>quantities</b>, and a quantity is always
a number <i>multiplied by a unit</i>: “the length is 3” is meaningless, “the length is 3 metres” is not.
You can treat units like algebraic symbols — multiply them, divide them, cancel them — which is the single
most useful habit in this whole paper.</p>

<p>Seven units are chosen as <b>base units</b> and everything else is built from them. You only need five
here:</p>
<div class="formula">metre      m     length
kilogram   kg    mass
second     s     time
ampere     A     electric current
kelvin     K     temperature</div>

<p>Every other unit is <b>derived</b>, meaning it is defined by an equation that ties it to the base units.
The definitions below are not extra facts to memorise — each one is just an equation you already know,
turned into units:</p>
<div class="formula">force (newton, N)        F = ma            N    = kg m s⁻²
energy (joule, J)        W = Fs            J    = N m    = kg m² s⁻²
power (watt, W)          P = E/t           W    = J s⁻¹  = kg m² s⁻³
pressure (pascal, Pa)    p = F/A           Pa   = N m⁻²  = kg m⁻¹ s⁻²
charge (coulomb, C)      Q = It            C    = A s
potential diff (volt, V) V = E/Q           V    = J C⁻¹  = kg m² s⁻³ A⁻¹
resistance (ohm, Ω)      R = V/I           Ω    = V A⁻¹  = kg m² s⁻³ A⁻²</div>

<p><b>The technique.</b> To find out what a complicated combination of units really is, replace each unit
by its base-unit definition and cancel. Worked example — is <b>W s² m⁻¹</b> a unit of impulse?</p>
<div class="formula">W s² m⁻¹ = (kg m² s⁻³)(s²)(m⁻¹)
         = kg · m²⁻¹ · s⁻³⁺²
         = kg m s⁻¹</div>
<p>Impulse is force × time, so its unit is N s = (kg m s⁻²)(s) = kg m s⁻¹. It matches, so W s² m⁻¹
<i>is</i> a unit of impulse. Nothing was computed numerically — only units were manipulated.</p>

<p><b>Traps.</b> Kilogram is a unit of <i>mass</i>, not of force or weight — the weight of 1 kg is about
9.8 N. And a symbol can mean different things in different places: W is both the unit watt and the
algebraic symbol for work or weight, so read the surrounding text.</p>`,
    used: "Q3 asks which option is <i>not</i> a unit of impulse. Every option has to be reduced to base units — including C V s m⁻¹, which needs the volt definition twice over.",
    q: ["R0-03"]
  },
  {
    id: "units-elim", m: "A", stage: 0, t: "Using units to throw away wrong options",
    pre: ["units"],
    one: "Before doing any physics on a multiple-choice question, check which options even have the right units — most papers have a question that dies this way.",
    body: `<p>In a five-option question the answer has to have the units of the quantity asked for. Work out those
units first, then test each option. Often three or four options collapse immediately, and the question
becomes trivial.</p>

<p><b>Example (Q1).</b> “Find the work done on the wire.” Work is energy, so the answer must come out in
joules, J = N m. The options are built from stress σ (unit Pa = N m⁻²), strain ε (no unit — it is a ratio
of two lengths), length L (m) and area A (m²):</p>
<div class="formula">σA      → (N m⁻²)(m²)      = N          ← a force, not an energy
σL      → (N m⁻²)(m)       = N m⁻¹     ← neither
εA      → (m²)             = m²        ← an area
½LAσε   → (m)(m²)(N m⁻²)   = N m = J   ← energy ✓</div>
<p>Only one option is an energy at all, so it must be the answer — and you never needed to know the
formula for strain energy. Note that a pure number like ½ carries no units, so it can never rescue an
option whose units are wrong.</p>

<p><b>Why it is safe.</b> An equation that is dimensionally wrong can never be physically right, so
eliminating by units never removes the true answer. It can only fail to narrow things down — in which
case you have lost five seconds.</p>

<p><b>Traps.</b> Angles, strains, refractive indices and ratios are dimensionless, so they vanish from the
check; and a constant such as ½, π or √2 is dimensionless too. Do not expect every option to contain
them.</p>`,
    used: "Q1: four of the five options are not energies, so the answer is forced without using the strain-energy formula.",
    q: ["R0-01"]
  },
  {
    id: "dimensions", m: "A", stage: 0, t: "Dimensions: checking and building formulas",
    pre: ["units"],
    one: "Dimensions are units with the sizes stripped away; they let you check an equation you half-remember and, better, build one you have never seen.",
    body: `<p>A <b>dimension</b> is the <i>kind</i> of quantity something is, written in square brackets and ignoring
numerical factors: [mass] = M, [length] = L, [time] = T, [current] = I. So the newton has dimensions
MLT⁻² whatever units you measure it in.</p>

<p><b>Use 1 — checking.</b> Both sides of any equation must have the same dimensions, and so must all the
terms you add or subtract. This catches sign errors, missing squares and mis-remembered formulas without
any numbers at all.</p>

<p><b>Use 2 — building.</b> If you are told “the pressure depends only on h, c and r”, then
p = (a constant) × h<sup>α</sup> c<sup>β</sup> r<sup>γ</sup> and you can find α, β, γ by matching
dimensions. Write down what you have:</p>
<div class="formula">[p] = M L⁻¹ T⁻²        (pressure = force/area)
[h] = M L² T⁻¹         ( Planck's constant: energy × time, or from E = hf )
[c] = L T⁻¹            (speed)
[r] = L                (separation)</div>
<p>Now substitute and collect up each of M, L, T:</p>
<div class="formula">M L⁻¹ T⁻² = (M L² T⁻¹)ᵃ (L T⁻¹)ᵇ (L)ᶜ
          = Mᵃ · L²ᵃ⁺ᵇ⁺ᶜ · T⁻ᵃ⁻ᵇ
M:   1 =  a
L:  −1 =  2a + b + c
T:  −2 = −a − b</div>
<p>Three equations, three unknowns: a = 1, then b = 1 from the T equation, then c = −4 from the L equation.
So p ∝ h c / r⁴ — the pressure falls off extremely fast, which is why the effect is only measurable at
sub-micron separations. No physics beyond the dimensions of p, h and c was needed.</p>

<p><b>Traps.</b> A dimensionally correct answer need not be right: any dimensionless constant (2, π, ½)
is invisible to this method, and 2πr and r are “the same” dimensionally. Also, sums of quantities must
match term by term — you cannot add a length to an area.</p>`,
    used: "Q13 is pure dimensional analysis: the Casimir pressure from h, c and r. The exponents fall out of three simultaneous equations.",
    q: ["R0-03", "R0-13"]
  },
  {
    id: "stdform", m: "A", stage: 0, t: "Standard form and arithmetic with powers of ten",
    pre: [],
    one: "Round 0 is non-calculator, so huge and tiny numbers are handled by tracking the power of ten separately from the digits.",
    body: `<p>A number in <b>standard form</b> is a × 10<sup>n</sup> where 1 ≤ a &lt; 10. The whole point is that you
can then do the digits and the powers separately:</p>
<div class="formula">multiply:  add the exponents      (2×10³)(3×10⁵) = 6×10⁸
divide:    subtract them          6×10⁸ / 3×10⁵ = 2×10³
power:     multiply the exponent  (2×10³)² = 4×10⁶
root:      halve the exponent     √(9×10⁶) = 3×10³</div>
<p>When a root or a division leaves you with 0.5 × 10<sup>n</sup>, borrow one power of ten from the
exponent: 0.5 × 10⁷ = 5 × 10⁶. And when multiplying gives 60 × 10⁸, carry: 6 × 10⁹.</p>

<p><b>Only keep one significant figure</b> in an estimate. In this paper the options are usually a factor
of 10⁵ apart, so anything more precise is wasted effort and wasted time.</p>

<p><b>A scale to anchor yourself.</b> It is worth knowing a few reference sizes so estimates can be
checked: an atom is about 10⁻¹⁰ m across, a nucleus about 10⁻¹⁵ m; a person is 10² kg and 10⁰ m; the
Earth is about 10⁷ m across and 10²⁵ kg; the Sun is 10³⁰ kg; the age of the universe is about 10¹⁷ s.
Anything you compute can be sanity-checked against these.</p>`,
    used: "Q10 needs the volume of the Earth, a mass, and a division by atomic mass — all in powers of ten, no calculator.",
    q: ["R0-10", "R0-15"]
  },
  {
    id: "estimate", m: "A", stage: 0, t: "Order-of-magnitude estimation (Fermi problems)",
    pre: ["stdform"],
    one: "An estimate is a calculation where you are only chasing the power of ten, so you are allowed to be brutally rough about everything else.",
    body: `<p>An <b>order-of-magnitude</b> question is not asking for the right answer; it is asking whether you can
get to within a factor of ten. The method is always the same:</p>
<ul>
<li>Write the quantity you want as a <b>product of things you can guess</b>.</li>
<li>Guess each one to the nearest power of ten only.</li>
<li>Multiply the powers of ten by adding the exponents.</li>
<li>Sanity-check against something you know.</li>
</ul>
<p><b>Example: how many atoms make up the Earth?</b> Split it into “how many moles” and “how many atoms
per mole”:</p>
<div class="formula">mass of Earth      ≈ 6 × 10²⁴ kg      → 10²⁵ kg
mass of one atom   ≈ 10⁻²⁶ kg         (roughly 10–100 × the proton mass 1.7×10⁻²⁷ kg)
number of atoms    ≈ 10²⁵ / 10⁻²⁶ = 10⁵¹</div>
<p>The options are 10⁵⁰, 10⁵⁵, 10⁶⁰, 10⁶⁵, 10⁷⁰ — the nearest is 10⁵⁰. Notice how far apart the options
are: being out by a factor of 3 changes nothing, being out by 10⁵ does. That is what makes this style of
question safe to answer quickly.</p>

<p><b>Traps.</b> Do not use the density of water for rock (rock is a few times denser — fine at this
precision); do not forget that the Earth is a sphere so the volume has a ⁴⁄₃π in it (about 4, i.e. less
than one power of ten — fine); and do not confuse the mass of an atom with the mass of a nucleon by more
than a factor of 100.</p>`,
    used: "Q10 is the classic: Earth mass ÷ mass per atom, with options spread five powers of ten apart.",
    q: ["R0-10"]
  },
  {
    id: "moles", m: "A", stage: 0, t: "Moles, Avogadro's number and counting atoms",
    pre: ["stdform"],
    one: "The mole is just a counting unit, like a dozen — it lets you go from a mass you can weigh to a number of atoms you cannot.",
    body: `<p>Atoms are far too small to count, so chemists count them in bundles. One <b>mole</b> is
6.02 × 10²³ particles — <b>Avogadro's number</b>, N<sub>A</sub>. That number is chosen so that one mole
of a substance has a mass in grams equal to its <b>relative atomic mass</b> (the number under the element
in the periodic table): carbon-12 has relative atomic mass 12, so 12 g of carbon is exactly one mole and
contains 6.02 × 10²³ atoms.</p>

<p>The only formula you need is a two-step conversion:</p>
<div class="formula">moles  n = mass / molar mass          (molar mass M in g mol⁻¹)
number of particles N = n × N_A</div>
<p>So a two-step chain: <b>mass → moles → number of atoms</b>. For a compound, add up the relative atomic
masses to get the molar mass (water: 2×1 + 16 = 18 g mol⁻¹), and remember that one mole of H₂O contains
one mole of oxygen atoms but two moles of hydrogen atoms.</p>

<p>Worked example — how many atoms in 54 g of water? Moles of water = 54/18 = 3 mol. Each water molecule
has 3 atoms, so moles of atoms = 9 mol. Number of atoms = 9 × 6 × 10²³ ≈ 5 × 10²⁴.</p>

<p><b>Traps.</b> Grams, not kilograms, go with molar mass — mixing them is the classic order-of-magnitude
error (a factor of 1000). And “number of atoms” is not “number of molecules”: multiply by the number of
atoms per molecule.</p>`,
    used: "Q10: converting the Earth's mass into a number of atoms runs through moles, or you can bypass it with one atomic mass.",
    q: ["R0-10"]
  },
  {
    id: "ratio", m: "A", stage: 0, t: "Ratio reasoning: let the constants cancel",
    pre: [],
    one: "Instead of computing two quantities and dividing, write each as a formula, divide the formulas, and watch the awkward constants disappear.",
    body: `<p>When a question asks “by what factor does X change?”, do <b>not</b> calculate X twice. Write X as a
formula, write the ratio, and cancel everything that did not change. This is faster, it is exact, and it
does not need a calculator.</p>

<p><b>Example (Q22, conical pendulum).</b> The string angle goes from 30° to 60° and you want the factor
by which the angular frequency must increase. The formula is ω² = g/(ℓ cos θ) — but you do not need to
believe that yet. Just take the ratio:</p>
<div class="formula">ω₂² / ω₁² = [ g / (ℓ cos 60°) ] / [ g / (ℓ cos 30°) ]
          = cos 30° / cos 60°
          = (√3/2) / (1/2)  = √3
so  ω₂ / ω₁ = √√3</div>
<p>g and ℓ vanished without ever being given values. That is the whole trick: <b>g, m, L, ρ, π and every
other shared constant cancels in a ratio</b>, and square roots of the ratio are how square-rooted
formulas behave.</p>

<p><b>Traps.</b> If the quantity sits under a square root, take the square root of the ratio too — this is
where most candidates lose the mark. And check the direction: “increased <i>by a factor</i>” means
multiply, whereas answering with the ratio the wrong way up gives you the reciprocal.</p>`,
    used: "Q9, Q21, Q22 and Q25 are all settled by a ratio rather than a calculation; Q9's answer 4:1 comes from f ∝ 1/R.",
    q: ["R0-09", "R0-22", "R0-25"]
  },
  {
    id: "surds", m: "A", stage: 0, t: "Surds and reciprocals without a calculator",
    pre: [],
    one: "A non-calculator paper leaves answers as √2, √3 and fractions of them; you need to manipulate them, not evaluate them.",
    body: `<p>A <b>surd</b> is just a square root left unresolved, like √2. You are expected to leave it that way and
compare it with the options symbolically. The handful of moves you need:</p>
<div class="formula">√(ab)  = √a · √b          √8 = √4·√2 = 2√2
√(a/b) = √a / √b          √(8/7) = 2√2 / √7
√a · √a = a              √2 · √2 = 2
1/√a = √a / a            1/√2 = √2/2</div>
<p>That last one, <b>rationalising the denominator</b>, is what turns an option like 4/√7 into
4√7/7 — the same number written the way the answer key wrote it. If your answer looks right but is not
listed, rationalise before you panic.</p>

<p>Approximate values worth memorising, for when you must compare numerically:
√2 ≈ 1.41, √3 ≈ 1.73, √5 ≈ 2.24, and 1/√2 ≈ 0.707.</p>

<p>Worked example (Q16): light takes (4/√7)·a/c to cross a cube. Is that the same as √(8/7)·a/c?
√(8/7) = √8/√7 = 2√2/√7 ≈ 2.83/2.65 ≈ 1.07, while 4/√7 ≈ 1.51. Different — so those two options are not
the same answer even though both contain √7.</p>

<p><b>Traps.</b> √(a + b) is <i>not</i> √a + √b — there is no rule for the root of a sum. And when you
square both sides to compare, keep the direction of any inequality straight.</p>`,
    used: "Q16's options are all surds times a/c; picking between them means manipulating roots, not evaluating them.",
    q: ["R0-16"]
  },
  {
    id: "logs", m: "A", stage: 0, t: "Logarithms and indices without a calculator",
    pre: ["stdform"],
    one: "A logarithm answers 'what exponent?', and that is exactly the question behind any 'how many half-lives?' problem.",
    body: `<p><b>log<sub>b</sub>(x)</b> asks: “b raised to what power gives x?” So log₂(8) = 3 because 2³ = 8. Logs
and powers are inverse operations, which gives you the two rules that matter here:</p>
<div class="formula">log(xy)  = log x + log y           multiplying → adding
log(xⁿ)  = n · log x               a power comes out front
log_b(x) = log x / log b           change of base</div>
<p><b>Why a log appears in decay questions.</b> Exponential decay is N = N₀ · 2<sup>−t/T</sup>: the
unknown t is in the <i>exponent</i>, and the only way to get an exponent down is to take a log of both
sides. “How long until one third has decayed?” means N/N₀ = 2/3, so:</p>
<div class="formula">2/3 = 2<sup>−t/T</sup>
log₂(2/3) = −t/T
t = −T · log₂(2/3) = T · log₂(3/2)</div>
<p>No calculator is needed to recognise the answer — the options are written with logs in them. But it is
worth knowing the size: log₂(3/2) = log₂3 − 1 ≈ 1.585 − 1 = 0.585, so the time is a bit more than half a
half-life, which makes sense because a third decaying is less than half decaying.</p>

<p><b>Traps.</b> log(a/b) = log a − log b, not log a / log b. And the base matters: log₂3 ≈ 1.585 while
log₁₀3 ≈ 0.477 — mixing them changes the answer by a factor of about 3.</p>`,
    used: "Q19: the time for one third to decay is T·log₂(3/2), and the options test whether you inverted the fraction.",
    q: ["R0-19"]
  },
  {
    id: "smallangle", m: "A", stage: 0, t: "Small-angle approximations — and knowing when you don't need them",
    pre: [],
    one: "For small angles in radians, sin θ ≈ θ, tan θ ≈ θ and cos θ ≈ 1 − θ²/2 — but sometimes an exact zero beats any approximation.",
    body: `<p>If θ is small and measured in <b>radians</b> (never degrees — this is the one place the unit really
matters), the trig functions simplify:</p>
<div class="formula">sin θ ≈ θ            tan θ ≈ θ            cos θ ≈ 1 − θ²/2</div>
<p>You can see where these come from: the series for sin θ is θ − θ³/6 + …, so for small θ the first term
dominates and the error is roughly θ³/6 — at θ = 0.1 rad the error is about 0.02 %, at θ = 0.5 rad about
2 %. That is why the approximations are safe for “small” angles and useless at 45°.</p>

<p><b>The more important skill is noticing when an approximation is unnecessary.</b> In Q18 two
projectiles are launched at (45° + α) and (45° − α) and you are asked for the difference in range. The
tempting move is to expand sin(2θ) for each angle and subtract — a page of algebra. But the exact range
formula is R = v² sin(2θ)/g, and:</p>
<div class="formula">sin(2(45°+α)) = sin(90° + 2α) = cos 2α
sin(2(45°−α)) = sin(90° − 2α) = cos 2α</div>
<p>They are <i>identically equal</i>, so the difference is exactly zero for every α, small or not. The
identity sin(90° ± x) = cos x is exact, so no approximation is involved at all.</p>

<p><b>Traps.</b> Using degrees in sin θ ≈ θ gives answers out by a factor of 57. And an approximation
gives you the <i>leading</i> behaviour only — if the true answer is exactly zero, approximating will
leave you with a small spurious term and the wrong option.</p>`,
    used: "Q18 says α is small, but the two ranges are <i>exactly</i> equal — the small-angle hint is a decoy.",
    q: ["R0-18"]
  },
  {
    id: "smallchange", m: "A", stage: 0, t: "Relating small changes through a constraint",
    pre: [],
    one: "When one length changes by a little, a linked length changes by a related little — differentiate the constraint to find how much.",
    body: `<p>Suppose two quantities are locked together by a fixed relationship, and you nudge one by a small
amount. The other shifts by an amount you can get without re-solving the whole problem: take the
relationship and look at how a <i>small change</i> in it must balance.</p>

<p><b>Example (Q24).</b> A ladder of length L leans with its foot x from the wall and its top at height y,
so x² + y² = L² always. Push the foot in by a small distance a (so x changes by −a) and ask how much the
middle rises. If x changes by δx and y by δy, then to first order:</p>
<div class="formula">x² + y² = L²
2x·δx + 2y·δy = 0
δy = −(x/y)·δx</div>
<p>At 45° to the vertical the ladder is at 45° to the horizontal too, so x = y and δy = −δx: pushing the
foot in by a raises the top by exactly a. The centre of mass is at the midpoint, so it rises by half of
that, a/2, and the gain in gravitational potential energy is mg·(a/2) = ½mga.</p>

<p><b>The general recipe:</b> write the constraint, replace each variable by “(change in it)”, throw away
any term containing two small changes multiplied together, and solve. Products of two small quantities
are negligible — that is the only calculus idea being used.</p>

<p><b>Traps.</b> The ratio x/y is <i>not</i> 1 in general; it is only 1 here because the angle happens to
be 45°. At a steeper angle the same push moves the top much less, which is why ladders get hard to move
as they approach vertical.</p>`,
    used: "Q24 is a small-change problem in disguise: constraint x²+y²=L², push a, half of it reaches the centre of mass.",
    q: ["R0-24"]
  },
  {
    id: "limits", m: "A", stage: 0, t: "Testing an algebraic answer on limiting cases",
    pre: [],
    one: "Put extreme numbers into the algebra — a mass ratio of 1, or infinity — and see which options still make physical sense.",
    body: `<p>When the answer is a formula in a symbol, try <b>extreme values</b> of that symbol. Real physics has to
behave sensibly at the extremes, and usually only one candidate formula does.</p>

<p><b>Example (Q12).</b> A body at rest explodes into masses m and am (a &gt; 1); total kinetic energy is E;
what is the kinetic energy of the <i>larger</i> mass? The options include E/(1+a), aE/(1+a²),
a²E/(1+a²), a³E/(1+a³) and E/a. Test a = 1: the two masses are then equal, so by symmetry each takes
exactly half the energy, E/2. Checking:</p>
<div class="formula">E/(1+a)     → E/2   ✓
aE/(1+a²)   → E/2   ✓
a²E/(1+a²)  → E/2   ✓
a³E/(1+a³)  → E/2   ✓
E/a         → E     ✗</div>
<p>Four survive, so try the other extreme, a → ∞ (an almost immovable heavy fragment). Then the heavy
fragment should barely move, so its kinetic energy should tend to 0:</p>
<div class="formula">E/(1+a)     → 0     ✓        (and the light one takes everything)
aE/(1+a²)   → 0     ✓
a²E/(1+a²)  → E     ✗
a³E/(1+a³)  → E     ✗</div>
<p>Two left. Now use the physics properly: momentum is zero before and after, so the two momenta are equal
and opposite, and KE = p²/2m means the lighter mass gets more energy. With equal p, KE ∝ 1/m, so the
energies split as a : 1 in favour of the light one — the heavy mass gets E/(1+a). Two limiting cases plus
one physical fact settles it.</p>

<p><b>Traps.</b> Symmetry tests (a = 1) and extreme tests (a → 0, a → ∞) are cheap and catch most wrong
options, but they rarely catch all of them. Use them to eliminate, then finish with physics.</p>`,
    used: "Q12's five options are separated by nothing but two limiting cases plus KE ∝ 1/m at fixed momentum.",
    q: ["R0-12", "R0-18"]
  },
  {
    id: "graphshape", m: "A", stage: 0, t: "Reasoning about the shape of a graph",
    pre: [],
    one: "You rarely need to plot a graph — you need its starting value, whether it jumps, its slope, and where it ends.",
    body: `<p>A “which graph is it?” question is answered by reading off four features, in this order:</p>
<ul>
<li><b>Value at t = 0</b> — is it zero, or does it start at some finite value?</li>
<li><b>Discontinuities</b> — does the quantity jump when something switches on? A force switching on makes
<i>acceleration</i> jump, but position and velocity stay continuous.</li>
<li><b>Slope</b> — is it increasing, decreasing, or changing sign? The slope of a graph is the rate of
change of the thing plotted.</li>
<li><b>Long-term behaviour</b> — does it settle to a constant, oscillate, or grow?</li>
</ul>
<p><b>Example (Q20).</b> Jerk j = da/dt for a bungee jumper. Before the rope goes taut the only force is
weight, so a = g constant and j = 0. The instant the rope becomes taut a spring force kx switches on, so a
jumps from g downward and j becomes a large negative spike — a jump <i>down</i> from zero. After that the
jumper performs simple harmonic motion: a = −ω²x plus gravity, so j = da/dt is proportional to velocity,
which oscillates. So the graph is: zero, jump down, then an oscillation — and the oscillation must start
at the moment of the jump.</p>

<p><b>The continuity rule is the key idea.</b> Position cannot jump (that would be infinite velocity),
velocity cannot jump (infinite force), but acceleration <i>can</i> jump because force can switch on
suddenly. So jerk, being the rate of change of acceleration, contains a spike or jump at that instant.</p>

<p><b>Traps.</b> Confusing “the graph crosses zero” with “the quantity is zero throughout”, and reading a
graph of a rate as if it were a graph of the quantity itself.</p>`,
    used: "Q20 is a graph-shape question about jerk: zero, then a jump, then an oscillation about a shifted value.",
    q: ["R0-20"]
  },
  {
    id: "counting", m: "A", stage: 0, t: "Counting combinations instead of listing them",
    pre: [],
    one: "'How many pairs?' is a formula, not a list — and listing is exactly how people lose this mark.",
    body: `<p>If you must choose an unordered <b>pair</b> from n objects, the count is:</p>
<div class="formula">number of pairs = C(n,2) = n(n−1)/2</div>
<p>Reason: there are n choices for the first object and (n−1) for the second, giving n(n−1) ordered pairs,
but each unordered pair has been counted twice (A then B, and B then A), so divide by 2.</p>

<p><b>Example (Q11).</b> Ten energy levels, all transitions allowed. A photon is emitted when an electron
drops between <i>two different</i> levels, so each photon energy corresponds to a pair of levels:
C(10,2) = 10 × 9 / 2 = 45. Note the question asks for the number of <i>unique energies</i>, and with
“all transitions possible” and no two gaps deliberately equal, each pair gives a distinct energy — so 45
is the maximum.</p>

<p><b>Traps.</b> Counting 10 × 9 = 90 means you counted each transition twice (3→1 and 1→3 are the same
photon, just emission versus absorption). Counting 100 or 110 means you included “transitions” from a
level to itself, which emit nothing. And “maximum” in the question is a hint that two gaps could
coincide — reducing the count — so the answer is the upper bound.</p>`,
    used: "Q11: ten levels, every transition allowed → C(10,2) = 45 photon energies.",
    q: ["R0-11"]
  },
  {
    id: "search", m: "A", stage: 0, t: "Exhaustive search of a small space",
    pre: [],
    one: "When there are only a handful of possibilities, list them all in a fixed order instead of hunting for a clever answer.",
    body: `<p>Some questions look like puzzles but have a tiny <b>search space</b> — a small number of combinations
to check. The discipline is to enumerate them <b>systematically</b> (largest first, or by the number of
pieces), so you can prove you have not missed one.</p>

<p><b>Example (Q6).</b> Resistors cost: 2 Ω £3, 3 Ω £1, 6 Ω £3, 8 Ω £4, 12 Ω £2; stock unlimited; make
exactly 4 Ω as cheaply as possible. Since 3 Ω is by far the cheapest, look there first:</p>
<div class="formula">three 3 Ω in parallel:  1/R = 1/3+1/3+1/3 = 1   → R = 1 Ω   ... then +3 Ω in series = 4 Ω
cost: 3 × £1 + £1 = £4</div>
<p>Could £3 do it? The only £3-and-under combinations are a single 2 Ω, a single 3 Ω, a single 6 Ω, or
two 3 Ω pieces (£2). Two 3 Ω give only 1.5 Ω in parallel or 6 Ω in series. None is 4 Ω, and no cheaper
combination exists — so £4 is the minimum, proved by exhausting the cheaper cases.</p>

<p><b>The habit.</b> Order the search by cost (or by count), rule out everything cheaper than your best
find, and stop. You have then not merely found an answer, you have shown there is no better one.</p>

<p><b>Traps.</b> Assuming a series chain is the only option (parallel combinations are usually where the
cheap answer hides), and forgetting “stock unlimited” means you may reuse the cheap value as often as you
like.</p>`,
    used: "Q6: cheapest exact 4 Ω. The answer hides in a parallel block of the cheapest resistor.",
    q: ["R0-06"]
  },
  {
    id: "readdiagram", m: "A", stage: 0, t: "Read the diagram before quoting a formula",
    pre: [],
    one: "Most wrong answers in this paper come from a correct formula applied to the wrong angle, direction or point.",
    body: `<p>Before writing anything down, spend ten seconds extracting the diagram's facts explicitly: <b>where is
the angle measured from</b> (the normal, or the surface?), <b>which way do the arrows point</b>, and
<b>which two points</b> is the question asking about.</p>

<p><b>Why it matters (Q2).</b> Snell's law is always quoted with angles from the <b>normal</b>. In the
figure for Q2 the angles θ₁ and θ₂ are drawn from the <b>surface</b>. If θ is measured from the surface
then the angle from the normal is (90° − θ), and sin(90° − θ) = cos θ. So the law becomes:</p>
<div class="formula">n₁ sin(angle from normal) = n₂ sin(angle from normal)
n₁ sin(90° − θ₁) = n₂ sin(90° − θ₂)
n₁ cos θ₁ = n₂ cos θ₂</div>
<p>Four of the five options are what you get by mixing up sines and cosines — the question is purely
testing whether you read the diagram. Note this is also physically sensible: if n₂ &lt; n₁ the ray bends
<i>away</i> from the normal, so θ₂ (from the surface) gets <i>smaller</i>, meaning cos θ₂ gets bigger —
which is exactly what n₁ cos θ₁ = n₂ cos θ₂ says.</p>

<p><b>Also check arrow directions.</b> In Q7 two particles move along a line and the question asks how far
one travels before they meet. If both arrows point the same way you need the <i>difference</i> of the
speeds (closing speed); if they point at each other you need the <i>sum</i>. A single arrow reversed
doubles or halves the answer.</p>

<p><b>Traps.</b> Assuming angles are always from the normal because that is how they are usually drawn,
and assuming a velocity arrow shows position rather than direction of motion.</p>`,
    used: "Q2 measures angles from the surface — every wrong option is a sine/cosine mix-up. Q7 depends on arrow directions.",
    q: ["R0-02", "R0-07"]
  },
  {
    id: "kinematics", m: "B", stage: 1, t: "Speed, velocity and constant acceleration",
    pre: [],
    one: "Motion is described by five quantities — s, u, v, a, t — linked by a small number of equations you can derive from a velocity–time graph.",
    body: `<p><b>Distance</b> is how far you actually travelled; <b>displacement</b> is how far you ended up from
where you started, with a direction. <b>Speed</b> is distance/time; <b>velocity</b> is displacement/time
and has a direction. For motion in a straight line at constant velocity:</p>
<div class="formula">s = v t        (displacement = velocity × time)</div>
<p>If velocity changes smoothly, <b>acceleration</b> a = (change in velocity)/(time taken). For constant
acceleration the four equations follow from one picture: a velocity–time graph is a straight line, the
area under it is the displacement, and its gradient is a.</p>
<div class="formula">v  = u + at
s  = ut + ½at²
s  = ½(u + v)t        (area of a trapezium)
v² = u² + 2as</div>
<p>Each one relates four of the five quantities s, u, v, a, t and leaves one out, so pick the equation
that omits the quantity you neither know nor want.</p>

<p><b>Example (Q7).</b> Constant speeds, so only s = vt is needed — no acceleration equation at all.
<b>Example (Q21).</b> A ring slides down a smooth rod from rest, so u = 0 and s = ½at² with a equal to the
component of g along the rod (see resolving vectors).</p>

<p><b>Traps.</b> “Starts from rest” means u = 0, and “comes to rest” means v = 0 — easy to miss. Signs
matter: choose a positive direction and give every velocity and acceleration the right sign, including
a = −g when up is positive.</p>`,
    used: "Q7 needs only s = vt; Q21 needs s = ½at² with a = g sin 30°.",
    q: ["R0-07", "R0-21"]
  },
  {
    id: "relvel", m: "B", stage: 1, t: "Relative velocity and closing speed",
    pre: ["kinematics"],
    one: "What matters in a catch-up problem is not how fast each object moves, but how fast the gap between them shrinks.",
    body: `<p>The <b>relative velocity</b> of B as seen by A is v<sub>B</sub> − v<sub>A</sub> (a vector subtraction).
In one dimension it is just a signed difference, and its magnitude tells you the rate at which the
<b>separation</b> changes — the <b>closing speed</b>.</p>
<div class="formula">same direction:   closing speed = |v_B − v_A|     (the difference)
opposite directions: closing speed = |v_B| + |v_A|   (the sum)
time to meet = initial separation / closing speed</div>
<p><b>Example (Q7).</b> Two particles 18 m apart, both moving along the same line in the same direction,
one at 6 m s⁻¹ and the other at 3 m s⁻¹ (as the diagram's arrows show). The gap closes at 6 − 3 =
3 m s⁻¹, so they meet after 18/3 = 6 s, and particle A has travelled 6 × 6 = 36 m. Doing it the long way
— writing s<sub>A</sub> = 6t and s<sub>B</sub> = 18 + 3t and solving — gives the same 36 m; relative
velocity is just the short cut.</p>

<p><b>Traps.</b> Adding speeds when the arrows point the same way (that halves the time), and using the
closing speed to compute the distance travelled by the wrong particle — the time is shared, the distance
is not.</p>`,
    used: "Q7: 18 m gap, closing speed 3 m s⁻¹, so 6 s and A travels 36 m.",
    q: ["R0-07"]
  },
  {
    id: "vectors-resolve", m: "B", stage: 1, t: "Resolving vectors into components",
    pre: ["kinematics"],
    one: "Any vector can be split into two perpendicular parts, and usually only one of those parts matters.",
    body: `<p>Force, velocity and acceleration are <b>vectors</b>: they have direction as well as size. In a
right-angled split, a vector of size F at angle θ to a direction has:</p>
<div class="formula">component along that direction      = F cos θ
component perpendicular to it       = F sin θ</div>
<p>The reason is just trigonometry on the triangle the vector makes — and the check is that when θ = 0 the
whole vector lies along the direction (cos 0 = 1), and when θ = 90° none of it does (cos 90° = 0).</p>

<p><b>On a slope (Q21).</b> A ring slides down a smooth rod at 30° to the horizontal. Gravity mg acts
straight down; split it into a part along the rod and a part perpendicular to it:</p>
<div class="formula">along the rod     : mg sin 30° = mg/2    ← this is what accelerates it
perpendicular     : mg cos 30° = mg√3/2  ← this is what the rod's normal reaction cancels</div>
<p>Because the ring cannot move perpendicular to the rod, the perpendicular forces must balance, and the
acceleration comes only from the along-the-rod component: a = g sin 30° = g/2. This is why things
accelerate more slowly down a gentle slope — the sin factor shrinks.</p>

<p><b>Traps.</b> Using sin where cos is needed. The check that never fails: as the angle goes to zero the
component along the slope must go to zero, so the along-slope component carries sin (of the angle to the
horizontal).</p>`,
    used: "Q21: the ring's acceleration down the rod is g sin 30° = g/2, from resolving weight along the rod.",
    q: ["R0-21"]
  },
  {
    id: "force", m: "C", stage: 1, t: "Force and Newton's laws of motion",
    pre: ["kinematics"],
    one: "A force is a push or pull; Newton's second law says the resultant force equals mass times acceleration, and that one line drives most of mechanics.",
    body: `<p>Forces are pushes or pulls: weight (gravity), tension in a string, the normal reaction from a surface,
friction, drag, and the force from a spring. They are vectors, so forces in different directions have to
be combined by adding components, not by adding sizes.</p>

<p><b>Newton's first law:</b> if the resultant force is zero, velocity is constant (including zero). Things
do not need a force to keep moving — they need one to <i>change</i> their motion.</p>

<p><b>Newton's second law:</b></p>
<div class="formula">F = m a        resultant force (N) = mass (kg) × acceleration (m s⁻²)</div>
<p>Strictly it is “resultant force = rate of change of momentum”, which reduces to ma when the mass is
constant. This is where the newton is defined: 1 N is what accelerates 1 kg at 1 m s⁻².</p>

<p><b>Newton's third law:</b> if A pushes B, B pushes A back with an equal force of the same type in the
opposite direction. The two forces act on <i>different</i> bodies, which is why they do not cancel out in
F = ma — never add a third-law pair into one equation of motion.</p>

<p><b>How to use it.</b> Pick a body, draw every force on it, resolve in the direction of motion, and set
the resultant equal to ma. If the body is in equilibrium, a = 0 and the forces simply balance.</p>

<p><b>Traps.</b> Forgetting weight when nothing mentions it — every body near Earth has mg downwards. And
“normal reaction” is not automatically equal to mg; it equals mg only when nothing else has a vertical
component.</p>`,
    used: "Q20 is Newton's second law with a spring force: the acceleration jumps when the rope goes taut, which is what makes the jerk graph jump.",
    q: ["R0-20"]
  },
  {
    id: "momentum", m: "C", stage: 1, t: "Momentum and impulse",
    pre: ["force"],
    one: "Momentum is 'mass in motion' (p = mv), and impulse — force × time — is exactly the amount by which momentum changes.",
    body: `<p><b>Momentum</b> is defined as:</p>
<div class="formula">p = m v        unit: kg m s⁻¹   (which is the same as N s)</div>
<p>It measures how hard an object is to stop: a heavy truck and a light cyclist at the same speed have
very different momenta. Momentum is a vector, so direction matters and opposite directions subtract.</p>

<p><b>Impulse</b> is force × time, and it equals the change in momentum. It comes straight out of Newton's
second law: F = ma = m(v−u)/t, so multiplying by t:</p>
<div class="formula">F t = m v − m u        impulse = change in momentum</div>
<p>That is why a longer stopping time means a smaller force — the physics behind crumple zones, airbags
and bending your knees when you land. The momentum change is fixed; spreading it over more time reduces
the force.</p>

<p><b>In Q3</b> the question is only about the <i>unit</i> of impulse, which can be written several ways
because it equals force × time:</p>
<div class="formula">N s                = (kg m s⁻²)(s)              = kg m s⁻¹
kg m s⁻¹           base units, the same thing
W s² m⁻¹           = (J s⁻¹)(s²)(m⁻¹) = (N m)(s)(m⁻¹) = N s
C V s m⁻¹          = (C)(J C⁻¹)(s)(m⁻¹) = J s m⁻¹ = N s
Pa m³              = (N m⁻²)(m³) = N m = J        ← energy, NOT impulse</div>
<p>Four of them reduce to N s; the odd one out is an energy.</p>

<p><b>Traps.</b> Momentum and kinetic energy are different quantities with different units (kg m s⁻¹
versus kg m² s⁻²) — do not use them interchangeably. And momentum has a direction: two equal and opposite
momenta give zero total, not double.</p>`,
    used: "Q3: four options are impulse units in disguise, one (Pa m³) is an energy.",
    q: ["R0-03"]
  },
  {
    id: "momcons", m: "C", stage: 1, t: "Conservation of momentum",
    pre: ["momentum"],
    one: "If no external force acts, the total momentum of a system never changes — even in an explosion where energy is released.",
    body: `<p>Conservation of momentum follows from Newton's third law: internal forces come in equal and opposite
pairs, so they cancel when you add up the whole system. Only an <b>external</b> force can change the
total.</p>
<div class="formula">total momentum before = total momentum after</div>
<p><b>Example (Q12).</b> A body at rest explodes into masses m and am. Before: momentum is zero. After:
the two momenta must still add to zero, so they are equal in size and opposite in direction:</p>
<div class="formula">p₁ = −p₂        so |p₁| = |p₂| = p</div>
<p>That single line is the whole physics of the explosion. The chemical energy released does not have to
be shared equally — momentum does, and it is shared equally-and-oppositely whatever the masses.</p>

<p><b>Traps.</b> Momentum is conserved in an explosion; kinetic energy is <i>not</i> (it increases, from
the chemical energy released). And “conserved” applies to the <i>system</i>: if a wall or the Earth is
pushing, momentum is not conserved for your two objects alone.</p>`,
    used: "Q12: the two fragments carry equal and opposite momentum, which is what fixes how the energy splits.",
    q: ["R0-12"]
  },
  {
    id: "ke", m: "C", stage: 1, t: "Work, energy and kinetic energy",
    pre: ["force", "momentum"],
    one: "Energy is the capacity to do work; a moving body carries ½mv², which can also be written p²/2m — and that second form is often the one you need.",
    body: `<p><b>Work</b> is done when a force moves its point of application:</p>
<div class="formula">W = F s cos θ        (θ is the angle between the force and the displacement)</div>
<p>Only the part of the force along the motion does work — carrying a bag horizontally does no work on it,
because the upward force is perpendicular to the horizontal motion (cos 90° = 0).</p>

<p><b>Kinetic energy</b> is the energy a body has because it is moving. Derivation: push a mass from rest
with constant force F over distance s, so work done = Fs; with v² = 2as and F = ma,</p>
<div class="formula">W = F s = (ma)(v²/2a) = ½ m v²
so   KE = ½ m v²        unit: kg m² s⁻² = J</div>
<p><b>The other form.</b> Because p = mv, substituting v = p/m gives:</p>
<div class="formula">KE = ½m(p/m)² = p² / 2m</div>
<p>This is the form that solves explosion and recoil questions, because momentum p is the thing that is
shared. At <b>fixed momentum, KE is inversely proportional to mass</b> — the light fragment gets most of
the energy.</p>

<p><b>Example (Q12).</b> Fragments m and am share momentum equally (size p). Their energies are
p²/2m and p²/2am, so they are in the ratio a : 1. Total E splits as E·a/(a+1) for the light one and
E/(a+1) for the heavy one — a clean result with no algebra beyond the ratio.</p>

<p><b>Traps.</b> KE depends on v², so doubling speed quadruples the energy. And KE is a scalar — it has no
direction, so “total kinetic energy” is a plain sum, never a vector subtraction.</p>`,
    used: "Q12: at equal momentum KE ∝ 1/m, so the heavy fragment gets only E/(1+a) of the energy.",
    q: ["R0-12"]
  },
  {
    id: "pe-gpe", m: "C", stage: 1, t: "Gravitational potential energy",
    pre: ["ke"],
    one: "Lifting something stores energy mgh; the path does not matter, only the change in height.",
    body: `<p>Near the Earth's surface gravity is nearly constant, so the weight of a mass m is mg downwards
(g ≈ 9.8 N kg⁻¹, the gravitational field strength). Lifting it through a height h needs a force mg over a
distance h:</p>
<div class="formula">work done = force × distance = mg × h
Δ(GPE) = m g h        unit: J</div>
<p>Because gravity is a <b>conservative</b> force, only the change in height counts, not the route taken —
climbing stairs or taking a ramp to the same floor stores the same energy.</p>

<p><b>Where is h measured from?</b> For an extended body, use the height of its <b>centre of mass</b>. That
is what makes Q24 work: pushing the ladder's foot in raises its top by a, but the centre of mass is
halfway along the ladder, so it rises by only a/2, and Δ(GPE) = mg × a/2 = ½mga.</p>

<p><b>Traps.</b> Using the height of one end instead of the centre of mass; forgetting the mass (GPE is
not just gh); and mixing up “height above the floor” with “change in height” — only the change appears in
energy bookkeeping.</p>`,
    used: "Q24: half of the ladder's rise reaches the centre of mass, giving Δ(GPE) = ½mga.",
    q: ["R0-24"]
  },
  {
    id: "hookeslaw", m: "C", stage: 1, t: "Hooke's law: the spring force",
    pre: ["force"],
    one: "Stretch a spring by x and it pulls back with force kx — linear in the extension, which is what makes oscillating systems so predictable.",
    body: `<p>For a spring or an elastic rope, the tension is proportional to how much it has been stretched beyond
its natural length:</p>
<div class="formula">F = k x        k = stiffness (N m⁻¹),  x = extension (m)</div>
<p>Doubling the stretch doubles the pull, up to the <b>limit of proportionality</b>; beyond that the
material stops behaving linearly and eventually deforms permanently. Note that x is the <i>extension</i>,
not the total length — a spring of natural length 0.5 m stretched to 0.7 m has x = 0.2 m.</p>

<p>The force always acts to restore the natural length, so as a vector it is:</p>
<div class="formula">F = −k x        (minus because it opposes the displacement)</div>
<p>That minus sign is the whole reason springs oscillate: pull it out and it pulls back, push it in and it
pushes out.</p>

<p><b>Energy stored.</b> Since the force grows from 0 to kx as you stretch, the average force is ½kx and
the work stored is:</p>
<div class="formula">E = ½ F x = ½ k x²        (also: the area under a force–extension graph)</div>

<p><b>Traps.</b> Using the total length as the extension; assuming k is the same for every spring (it
depends on material and dimensions); and applying F = kx to a <i>slack</i> rope — a slack rope exerts no
force at all until it goes taut, which is exactly the switch that makes the bungee question interesting.</p>`,
    used: "Q20: before the rope is taut there is no spring force at all; the moment it goes taut, kx switches on and the acceleration jumps.",
    q: ["R0-20"]
  },
  {
    id: "shm", m: "C", stage: 1, t: "Simple harmonic motion",
    pre: ["hookeslaw", "force"],
    one: "Whenever the restoring force is proportional to displacement, the motion is sinusoidal with ω² = k/m — and you can write that down without solving anything.",
    body: `<p>Put Hooke's law into Newton's second law:</p>
<div class="formula">F = −kx   and   F = ma
ma = −kx      →      a = −(k/m) x</div>
<p>That is the defining equation of <b>simple harmonic motion</b>: acceleration is proportional to
displacement and directed back towards the centre. Comparing with the standard form a = −ω²x:</p>
<div class="formula">ω² = k/m              ω = √(k/m)          T = 2π/ω = 2π√(m/k)</div>
<p>ω is the <b>angular frequency</b> in rad s⁻¹; the frequency in hertz is f = ω/2π and the period is
T = 1/f. Notice that the amplitude does <b>not</b> appear anywhere — a pendulum-style oscillator takes the
same time for a big swing as for a small one.</p>

<p><b>The shortcut.</b> You never need to solve the differential equation. If you can show
a = −(constant) × x, then ω² is that constant, read straight off. This is the standard way to handle
“find the period of…” questions.</p>

<div class="formula">displacement  x = A cos(ωt)          velocity  v = −Aω sin(ωt)
acceleration  a = −Aω² cos(ωt) = −ω²x</div>
<p>So velocity is zero at the extremes and maximum at the centre, while acceleration is maximum at the
extremes and zero at the centre — they are a quarter-cycle out of step.</p>

<p><b>Traps.</b> Confusing ω (rad s⁻¹) with f (Hz) — they differ by 2π. And SHM requires the force to be
<i>proportional</i> to displacement; a constant force like gravity does not produce SHM, it only shifts
the equilibrium position.</p>`,
    used: "Q20: once the rope is taut the jumper is an oscillator with ω² = k/m, so acceleration — and therefore jerk — oscillates.",
    q: ["R0-20"]
  },
  {
    id: "moments", m: "C", stage: 1, t: "Moments and the principle of moments",
    pre: ["force"],
    one: "A force far from a pivot turns things more effectively: moment = force × perpendicular distance, and equilibrium needs the turning effects to balance.",
    body: `<p>The <b>moment</b> (torque) of a force about a point measures its turning effect:</p>
<div class="formula">moment = F × d        d = perpendicular distance from the pivot to the line of action of F
unit: N m   (note: not joules — a moment is not energy, even though the units look the same)</div>
<p>The word <b>perpendicular</b> is what people forget: d is not the distance to where the force is
applied, it is the shortest distance from the pivot to the force's <i>line</i>. Equivalently,
moment = F × (distance along the object) × sin(angle between them).</p>

<p><b>Principle of moments.</b> If a body is not rotating, the total clockwise moment about any point
equals the total anticlockwise moment about that point. You are free to choose the point — and the
clever choice is a point where an unknown force acts, because a force through the pivot has zero moment
and drops out of the equation.</p>

<p><b>Example (Q21).</b> A rod hinged at O is held by a vertical string at its far end ℓ away, at 30° to
the horizontal. Take moments about O so the hinge reaction disappears. The string's tension T is vertical
and acts at horizontal distance ℓ cos 30° from O, so its moment is T·ℓ cos 30°. The ring pushes on the
rod perpendicular to it with force mg cos 30° at distance (ℓ − s) along the rod, where s is how far it
has slid, giving moment mg cos 30°·(ℓ − s). Equating:</p>
<div class="formula">T · ℓ cos 30° = mg cos 30° (ℓ − s)      →      T = mg (1 − s/ℓ)</div>
<p>The cos 30° cancels — which is why the answer is tidier than the setup suggests.</p>

<p><b>Traps.</b> Using the distance along the rod instead of the perpendicular distance; and forgetting a
force entirely. Draw every force first.</p>`,
    used: "Q21: moments about the hinge give T = mg(1 − s/ℓ), and with s = ¼gt² that becomes T = mg(1 − gt²/4ℓ).",
    q: ["R0-21"]
  },
  {
    id: "com", m: "C", stage: 1, t: "Centre of mass",
    pre: ["moments"],
    one: "For almost every purpose, an extended object behaves as if all its mass were concentrated at one point — the centre of mass.",
    body: `<p>The <b>centre of mass</b> is the balance point of a body. Support it there and the body balances; push
it there and the body accelerates without rotating. Formally it is the mass-weighted average position:</p>
<div class="formula">x_cm = ( m₁x₁ + m₂x₂ + … ) / ( m₁ + m₂ + … )</div>
<p>For a <b>uniform</b> body the centre of mass is at its geometric centre: the middle of a uniform rod,
the centre of a uniform disc, the meeting point of the medians of a uniform triangle.</p>

<p><b>Why it matters</b>, in three separate places on this paper:</p>
<ul>
<li><b>Gravity acts there.</b> The weight of a body is a single force mg through the centre of mass, and
its gravitational potential energy is mg × (height of the centre of mass) — Q24.</li>
<li><b>Toppling starts there.</b> A body topples when its centre of mass passes beyond the edge of its
support — Q14.</li>
<li><b>Moments use it.</b> In a moment calculation the weight of an extended body acts as one force at
the centre of mass — Q21, Q24.</li>
</ul>

<p><b>Traps.</b> Assuming the centre of mass is at the geometric centre for a <i>non-uniform</i> body (a
ladder with a heavy rung, a half-full glass); and using the position of one end of a body instead of its
centre when computing GPE.</p>`,
    used: "Q14 (toppling) and Q24 (GPE change) both turn on where the centre of mass sits.",
    q: ["R0-14", "R0-24"]
  },
  {
    id: "statics", m: "C", stage: 1, t: "Static equilibrium",
    pre: ["force", "moments", "vectors-resolve"],
    one: "For a body at rest, three conditions: forces up = forces down, forces left = forces right, and clockwise moments = anticlockwise moments.",
    body: `<p><b>Equilibrium</b> means zero acceleration and zero rotation, so both Newton's second law and the
principle of moments apply with a = 0:</p>
<div class="formula">ΣF (any direction) = 0        resolve in two perpendicular directions
Σmoments (any point) = 0      about any point you like</div>
<p>You get three independent equations, which is why a statics problem can fix three unknowns. Choosing
the point for moments well — through the line of action of an unwanted force — deletes that force from
the equation entirely.</p>

<p><b>Three-force bodies.</b> If exactly three non-parallel forces hold a body in equilibrium, their lines
of action must all pass through a single point. (If they did not, one of them would have a moment about
the intersection of the other two and the body would rotate.) This is a fast way to sketch the direction
of an unknown reaction at a hinge.</p>

<p><b>The standard example</b> is a ladder against a wall: weight mg at the centre, a normal reaction from
the wall (horizontal, if the wall is smooth), and at the floor a normal reaction (vertical) plus friction
(horizontal). Resolve horizontally and vertically, take moments about the foot of the ladder, and the
friction needed at the base falls out.</p>

<p><b>Traps.</b> Putting friction at the wrong end — a <i>smooth</i> wall or floor means no friction there
at all, and “smooth” is a word to circle when you read the question. And forgetting that a reaction from
a surface is perpendicular to that surface.</p>`,
    used: "Q24's ladder and Q14's biscuit are both equilibrium problems; Q21's rod uses moments about the hinge.",
    q: ["R0-14", "R0-21"]
  },
  {
    id: "toppling", m: "C", stage: 1, t: "Toppling and the support condition",
    pre: ["com", "statics"],
    one: "An object does not fall when it is off-centre — it falls when its centre of mass is no longer above its area of support.",
    body: `<p>Weight acts vertically through the centre of mass. If that vertical line — the <b>plumb line</b> — falls
inside the region of contact with the support, the support pushes up on the near side and the body stays
put. Move the body until the plumb line reaches the <b>edge</b> of the support and it is on the point of
toppling; push further and the weight now has a moment about that edge which rotates the body off.</p>
<div class="formula">limiting case:  centre of mass is directly above the pivot edge
beyond that:    the moment of the weight about the edge is unopposed  →  it topples</div>
<p><b>Example (Q14).</b> A thin disc of radius b balances on the rim of a cup of radius 3b/2, placed as far
from the cup's centre as possible. The disc's centre must sit above the rim circle; two circles of radii
b and 3b/2 intersect, and the disc is at the limit when its centre of mass is exactly above the point of
contact. The geometry gives a maximum offset of ((3 − √5)/2)·b — the answer comes from the triangle
formed by the two centres and the contact point, using Pythagoras on a right angle at the contact.</p>

<p><b>The general method.</b> Identify the pivot edge (the last point of contact), then write the
condition “horizontal distance from the pivot to the centre of mass = 0” at the limit.</p>

<p><b>Traps.</b> Using the centre of the support rather than its edge; and forgetting that on a curved rim
there is effectively a single contact point, which becomes the pivot.</p>`,
    used: "Q14: the biscuit topples when its centre passes the contact point on the cup's rim.",
    q: ["R0-14"]
  },
  {
    id: "circular", m: "D", stage: 1, t: "Circular motion: angular speed and centripetal force",
    pre: ["force", "vectors-resolve"],
    one: "Anything moving in a circle is constantly accelerating towards the centre, so something must be pushing it there with F = mv²/r.",
    body: `<p>Even at constant speed, circular motion is accelerated motion, because the <b>direction</b> of the
velocity keeps changing. The acceleration points to the centre — <b>centripetal</b>, “centre-seeking”.</p>

<p>Two descriptions of the same motion. If the body goes round once in period T:</p>
<div class="formula">ω = 2π / T = 2π f        angular speed, rad s⁻¹
v = r ω = 2πr / T        speed along the arc</div>
<p>One full turn is 2π radians, which is why 2π keeps appearing. Then the acceleration and the required
force are:</p>
<div class="formula">a = v² / r = r ω²        towards the centre
F = m v² / r = m r ω²    the resultant force towards the centre</div>
<p><b>Centripetal force is not a new force.</b> It is the <i>name</i> for whatever real force or
combination of forces points to the centre — tension in a string, gravity on a satellite, friction on a
car in a bend, or the horizontal component of tension in a conical pendulum. Never add “centripetal
force” to a free-body diagram as if it were an extra push.</p>

<p><b>Traps.</b> Using diameter instead of radius; mixing ω with f (they differ by 2π); and thinking there
is an outward “centrifugal force” — in an inertial frame there is not; the body is simply trying to go
straight.</p>`,
    used: "Q22: the conical pendulum's horizontal force is the centripetal force, and ω² = g/(ℓ cos θ) comes straight from it.",
    q: ["R0-22"]
  },
  {
    id: "conical", m: "D", stage: 1, t: "The conical pendulum",
    pre: ["circular", "vectors-resolve"],
    one: "A string tracing out a cone: the vertical component of tension holds the bob up, the horizontal component drives it round, and the ratio gives ω² = g/(ℓ cos θ).",
    body: `<p>A bob on a string of length ℓ moves in a horizontal circle with the string at a fixed angle θ to the
vertical. Two facts, and then it is just algebra:</p>
<div class="formula">vertical   (no vertical acceleration):  T cos θ = mg
horizontal (this is the centripetal force): T sin θ = m r ω²   with r = ℓ sin θ</div>
<p>Divide the second by the first to eliminate both T and m:</p>
<div class="formula">tan θ = r ω² / g         and since r = ℓ sin θ
(sin θ / cos θ) = ℓ sin θ · ω² / g
ω² = g / (ℓ cos θ)</div>
<p>Two things worth noticing. The <b>mass cancels</b> — a heavy bob and a light one behave identically. And
as θ → 90° the string becomes horizontal, cos θ → 0 and ω → ∞: you can never actually make a string
perfectly horizontal, because some tension is always needed to hold the weight up.</p>

<p><b>Example (Q22).</b> θ goes from 30° to 60°. Only ω² ∝ 1/cos θ matters:</p>
<div class="formula">ω₂²/ω₁² = cos 30° / cos 60° = (√3/2)/(1/2) = √3
ω₂/ω₁   = √√3</div>
<p>Note the double root: the formula gives ω², so a ratio of ω² means a square root to get ω, and √3
under another root is √√3 — the kind of nested surd the answer key leaves alone.</p>

<p><b>Traps.</b> Using the string length ℓ as the circle radius (the radius is ℓ sin θ); and forgetting to
take the square root when the question asks for ω rather than ω².</p>`,
    used: "Q22: factor √√3, because ω² ∝ 1/cos θ and cos 30°/cos 60° = √3.",
    q: ["R0-22"]
  },
  {
    id: "projectile", m: "B", stage: 1, t: "Projectiles: range and complementary angles",
    pre: ["kinematics", "vectors-resolve"],
    one: "Split the motion: horizontal at constant speed, vertical under gravity. The range formula then shows that 45° + α and 45° − α land in the same place.",
    body: `<p>A projectile is two independent problems sharing a clock. Horizontally there is no acceleration, so
x = v cos θ · t. Vertically the acceleration is −g, so y = v sin θ · t − ½gt². Eliminate t using the time
of flight (from y = 0 again) and you get the <b>range</b>:</p>
<div class="formula">time of flight  t = 2 v sin θ / g
range           R = (v cos θ) t = v² sin(2θ) / g</div>
<p>sin(2θ) is largest when 2θ = 90°, i.e. θ = 45° — the familiar result that 45° gives the maximum range,
and R<sub>max</sub> = v²/g.</p>

<p><b>The symmetry that Q18 uses.</b> Because the formula contains sin(2θ), two angles that add to 90°
give the same range:</p>
<div class="formula">θ₁ = 45° + α  →  2θ₁ = 90° + 2α  →  sin(90° + 2α) = cos 2α
θ₂ = 45° − α  →  2θ₂ = 90° − 2α  →  sin(90° − 2α) = cos 2α</div>
<p>Identical, for every α. So the difference in range is exactly <b>zero</b> — not “small”, not
“approximately zero”. The question tells you α is small, which is a hint to reach for approximations, but
an exact identity beats an approximation every time.</p>

<p><b>Traps.</b> Assuming air resistance is included (it is not, unless stated); using v sin θ for the
horizontal component; and thinking the maximum-height formula is needed here — it is not.</p>`,
    used: "Q18: complementary launch angles give identical ranges, so the difference is exactly 0.",
    q: ["R0-18"]
  },
  {
    id: "stressstrain", m: "E", stage: 2, t: "Stress, strain and the Young modulus",
    pre: ["force"],
    one: "Stress is force per area and strain is extension per length; their ratio (below the limit of proportionality) is the Young modulus — a stiffness that depends only on the material.",
    body: `<p>Pull on a wire and it stretches. To talk about it independently of how thick or how long the wire is,
two ratios are defined:</p>
<div class="formula">stress  σ = F / A          force per cross-sectional area, unit Pa (= N m⁻²)
strain  ε = ΔL / L         extension per original length, no unit (a ratio of two lengths)</div>
<p>Strain is dimensionless and is often quoted as a percentage. These two let you compare a thick short
wire with a thin long one of the same material.</p>

<p><b>Hooke's law in material form.</b> Up to the limit of proportionality, stress is proportional to
strain, and the constant is a property of the <i>material</i>, not the sample:</p>
<div class="formula">E = σ / ε        the Young modulus, unit Pa</div>
<p>Steel is about 2 × 10¹¹ Pa, copper about 1 × 10¹¹ Pa, rubber far lower. A large Young modulus means a
stiff material — a big stress for a small strain. You can connect it back to the spring constant: since
σ = F/A and ε = ΔL/L,</p>
<div class="formula">F/A = E · ΔL/L     →     F = (EA/L) · ΔL
so k = EA / L      — thicker and shorter means stiffer, as expected</div>

<p><b>Traps.</b> Confusing stress with force (stress is per unit area) — this is exactly what Q1 tests.
And strain is the extension divided by the <i>original</i> length, not the final length.</p>`,
    used: "Q1's options are combinations of σ, ε, L and A; knowing that σ is a pressure and ε is dimensionless is half the question.",
    q: ["R0-01"]
  },
  {
    id: "strainenergy", m: "E", stage: 2, t: "Elastic strain energy",
    pre: ["stressstrain", "ke"],
    one: "The energy stored in a stretched wire is the area under its force–extension graph: ½FΔL, which in material form is ½σε × volume.",
    body: `<p>While you stretch a wire the force is not constant — it grows from 0 up to the final force F as Hooke's
law is obeyed. So the work done is (average force) × (extension):</p>
<div class="formula">W = ½ F ΔL        (the area of the triangle under a straight force–extension graph)</div>
<p>Now rewrite it in material language. F = σA and ΔL = εL, so:</p>
<div class="formula">W = ½ (σA)(εL) = ½ σ ε (A L) = ½ σ ε V        where V = AL is the volume of the wire</div>
<p>That is the answer to Q1: ½LAσε. And it makes sense dimensionally — σ is a pressure (J m⁻³) and ε is
dimensionless, so σεV is an energy: <i>energy stored per unit volume</i> is a neat way to read ½σε.</p>

<p><b>Two ways to check it.</b> Units: Pa × (no unit) × m³ = N m⁻² × m³ = N m = J ✓. Limiting case: double
the stress at fixed strain and you store twice as much energy, which the ½ × σ × ε × V form gives
directly.</p>

<p><b>Traps.</b> Using F·ΔL instead of ½F·ΔL — the half is there because the force builds up from zero,
and forgetting it is the single commonest error in this topic. And strain energy is not the same as the
total work if the material has passed its elastic limit: some energy is dissipated as heat and the wire
stays stretched.</p>`,
    used: "Q1: ½LAσε, which is ½ × stress × strain × volume — or just ½ × final force × extension.",
    q: ["R0-01"]
  },
  {
    id: "specificheat", m: "J", stage: 2, t: "Specific heat capacity: Q = mcΔT",
    pre: ["ke"],
    one: "Warming something up takes energy proportional to its mass and to the temperature rise; the constant of proportionality is the material's specific heat capacity.",
    body: `<p><b>Heat</b> is energy in transit because of a temperature difference; <b>temperature</b> is how hot
something is. They are different quantities, and the equation linking them is:</p>
<div class="formula">Q = m c ΔT        Q energy (J),  m mass (kg),  ΔT temperature change (K or °C),
c = specific heat capacity, J kg⁻¹ K⁻¹</div>
<p>Water's value is unusually large, 4200 J kg⁻¹ K⁻¹: it takes 4200 J to warm 1 kg of water by 1 °C. That
is why water is used as a coolant, and why coastal climates are mild. Copper is about 385, so it warms up
more than ten times faster for the same energy input.</p>

<p>Note that a change in kelvin equals the same change in degrees Celsius, so ΔT can be read straight off
a Celsius thermometer — you only need the absolute scale for gas laws.</p>

<p><b>Energy balance.</b> In an insulated cup, energy lost by the hot body = energy gained by the cold one.
Write one side as mcΔT for each body and equate. This is the whole method for “what is the final
temperature?” questions.</p>

<p><b>Traps.</b> Using grams instead of kilograms when c is quoted per kilogram — a factor of 1000. And
forgetting that if the final temperature is not given, the hot body cools only as far as the shared final
temperature, not to 0 °C.</p>`,
    used: "Q15: the water can only give up mcΔT = 0.5 × 4200 × 20 = 42 kJ before it reaches 0 °C.",
    q: ["R0-15"]
  },
  {
    id: "latentheat", m: "J", stage: 2, t: "Specific latent heat: Q = mL",
    pre: ["specificheat"],
    one: "Melting needs energy with no change in temperature at all — the latent heat — so a two-stage calculation is needed whenever ice is involved.",
    body: `<p>Put energy into ice at 0 °C and it melts into water at 0 °C: the temperature does not budge, yet
energy was absorbed. It went into breaking the bonds of the solid structure, not into making molecules
move faster (which is what temperature measures). The energy per kilogram is the <b>specific latent
heat</b>:</p>
<div class="formula">Q = m L          L (fusion, ice → water) ≈ 3.3 × 10⁵ J kg⁻¹
L (vaporisation, water → steam) ≈ 2.3 × 10⁶ J kg⁻¹</div>
<p>Latent heat of fusion for ice is about 80 times the energy needed to warm the same water by 1 °C —
which is why melting ice is so effective at cooling drinks, and why “just a few ice cubes” is a
substantial energy sink.</p>

<p><b>Example (Q15).</b> 500 g of water at 20 °C, ice cubes of 25 g at 0 °C, and we want the largest
number of cubes such that all the ice just melts (final temperature 0 °C). Energy available from the
water cooling to 0 °C:</p>
<div class="formula">Q = mcΔT = 0.5 × 4200 × 20 = 42 000 J
per cube: Q = mL = 0.025 × 3.3×10⁵ = 8250 J
number = 42 000 / 8250 = 5.09  →  5 whole cubes</div>
<p>The answer is 5, not 6: the sixth cube would not fully melt, so the final state would be ice and water
together at 0 °C — which contradicts “so that all the ice melts”. Whenever a question asks for a number
of discrete objects, <b>round down</b> and then sanity-check the boundary case.</p>

<p><b>Traps.</b> Forgetting the second stage — once the ice has melted it is water at 0 °C and must then
be warmed to the final temperature, which costs extra mcΔT. (Here the final temperature is 0 °C, so that
stage is free.) And using the latent heat of vaporisation when the change is melting.</p>`,
    used: "Q15: 5 cubes, because 6 would need 49.5 kJ and only 42 kJ is available.",
    q: ["R0-15"]
  },
  {
    id: "waves", m: "F", stage: 3, t: "What a wave is: wavelength, frequency and speed",
    pre: [],
    one: "A wave carries energy without carrying matter; wavelength, frequency and speed are tied together by v = fλ.",
    body: `<p>In a wave, the particles of the medium oscillate about fixed positions while the <b>pattern</b> travels.
Nothing is transported except energy and information.</p>
<div class="formula">λ  wavelength (m)     distance between two successive crests
f  frequency (Hz)     number of complete cycles per second
T  period (s)         time for one cycle, T = 1/f
v  speed (m s⁻¹)      how fast the pattern travels</div>
<p>In one period the pattern advances one wavelength, so speed = distance/time = λ/T = fλ:</p>
<div class="formula">v = f λ</div>
<p>For light in a vacuum, v = c = 3.0 × 10⁸ m s⁻¹ and that never changes; changing the medium changes λ,
not f. <b>Frequency is set by the source and stays constant when a wave crosses a boundary</b> — this is
the fact that makes refraction work.</p>

<p><b>Traps.</b> Thinking the medium travels with the wave (a floating boat bobs up and down, it does not
surf along); and thinking frequency changes at a boundary — it is the wavelength that changes.</p>`,
    used: "Q5's path difference has to be compared with λ = 1.8 m; Q9's first harmonic uses v = fλ.",
    q: ["R0-05"]
  },
  {
    id: "superposition", m: "F", stage: 3, t: "Superposition and interference",
    pre: ["waves"],
    one: "When two waves overlap the displacements simply add — and whether the result is big or zero depends on how the crests line up.",
    body: `<p>The <b>principle of superposition</b> says that where two waves meet, the resultant displacement is the
algebraic sum of the individual displacements. Nothing else happens: the waves pass through each other
and carry on unchanged.</p>
<div class="formula">crest meets crest  (in phase)        →  amplitudes add      →  constructive
crest meets trough (out of phase)    →  amplitudes subtract →  destructive</div>
<p>With two equal amplitudes A, perfect constructive interference gives 2A and perfect destructive gives
0. With unequal amplitudes you never get complete cancellation.</p>

<p><b>Partial interference.</b> If the waves are part-way between in-phase and out-of-phase, you get
something in between — “mostly constructive” or “mostly destructive”. That is what Q5 asks about, which
is why “not enough information” is the wrong answer: the path difference in wavelengths tells you exactly
where on the spectrum you are.</p>

<p><b>Traps.</b> Thinking waves “bounce off” each other (they pass through); and assuming interference
needs two separate sources — a single source split along two paths (as here) works perfectly well, which
is the whole point of coherence.</p>`,
    used: "Q5: two waves from the same source arrive with a path difference of 7.5 m out of λ = 1.8 m.",
    q: ["R0-05"]
  },
  {
    id: "pathphase", m: "F", stage: 3, t: "From path difference to phase difference",
    pre: ["superposition", "waves"],
    one: "Divide the path difference by the wavelength: the whole number tells you nothing, and the fraction tells you exactly how the waves combine.",
    body: `<p>If one wave has travelled further than the other by a distance Δx, that extra distance corresponds to a
certain fraction of a cycle. Since one full wavelength is one full cycle:</p>
<div class="formula">phase difference (in cycles) = Δx / λ
phase difference (radians)   = 2π · Δx/λ
phase difference (degrees)   = 360° · Δx/λ</div>
<p>The <b>whole-number part is irrelevant</b>: being 3 wavelengths behind is the same as being in step,
because the wave repeats. Only the <b>fractional part</b> matters:</p>
<div class="formula">fraction 0      →  in phase             →  fully constructive
fraction ½      →  exactly out of step  →  fully destructive
fraction ¼ or ¾ →  quarter-cycle off    →  in between</div>
<p><b>Example (Q5).</b> λ = 1.8 m and Δx = 7.5 m:</p>
<div class="formula">7.5 / 1.8 = 4.166…
whole part: 4      (irrelevant)
fraction:   0.167 ≈ 1/6 of a cycle</div>
<p>One sixth of a cycle is close to 0 rather than to ½, so the waves arrive nearly in step — the result is
<b>mostly constructive</b>. A quick way to see it: 7.5 m is 4 whole wavelengths (7.2 m) plus 0.3 m, and
0.3 m out of 1.8 m is a sixth of a cycle — far from the half-cycle needed for cancellation.</p>

<p><b>Traps.</b> Forgetting to discard the whole number of wavelengths (the commonest error); and converting
to radians when the question only needs the fraction — the fraction of a cycle is the direct answer.</p>`,
    used: "Q5: 7.5/1.8 = 4.17 wavelengths → fraction 0.17 → mostly constructive.",
    q: ["R0-05"]
  },
  {
    id: "coherence", m: "F", stage: 3, t: "Coherence: what two sources need in order to interfere",
    pre: ["superposition", "waves"],
    one: "Two sources only produce a steady interference pattern if they keep a constant phase relationship — which in practice means the same frequency and a fixed phase difference.",
    body: `<p><b>Coherence</b> means a constant phase difference. Two sources are coherent when they have the same
frequency and their phase difference does not drift. If the phase difference wanders, the interference
pattern shifts around faster than the eye (or detector) can follow and you just see an average — no
visible pattern.</p>

<p>Because of this, interference in school physics is almost always produced by taking <b>one</b> source
and splitting it into two paths: a single lamp behind a double slit, a single laser in a split-beam
arrangement, or — as in Q5 — the same source sending waves along two routes to the same detector.
Splitting one source guarantees coherence, because the two copies start in phase.</p>

<p>The reason two separate lamps cannot do it: their atoms emit in random, unrelated bursts, so the phase
difference changes randomly millions of times a second.</p>

<p><b>Traps.</b> Thinking “coherent” means “same amplitude” (it does not — equal amplitude is a separate
condition that only decides whether cancellation is <i>complete</i>); and thinking lasers are coherent
because they are bright (they are coherent because of how the light is generated).</p>`,
    used: "Q5 states both waves come from the same source — that is what licenses the interference calculation.",
    q: ["R0-05"]
  },
  {
    id: "standingwaves", m: "F", stage: 3, t: "Standing waves on a string",
    pre: ["waves", "superposition"],
    one: "Fix a string at both ends and only certain wavelengths fit; the lowest is half a wavelength per loop, giving f₁ = (1/2L)√(T/μ).",
    body: `<p>Send waves along a string fixed at both ends and they reflect from the ends. The forward and backward
waves superpose, and at most frequencies the result is a mess. At certain frequencies, though, the
reflected wave lines up with the incoming one and a <b>standing wave</b> forms: a pattern that does not
travel, with points that never move (<b>nodes</b>) and points of maximum swing (<b>antinodes</b>).</p>

<p>The condition is that the string length must contain a whole number of half-wavelengths, because a
node is needed at each fixed end:</p>
<div class="formula">L = n λ / 2        n = 1, 2, 3, …
λ_n = 2L / n
f_n = v / λ_n = n v / (2L)        with  v = √(T/μ)</div>
<p>The lowest, n = 1, is the <b>first harmonic</b> (fundamental): one loop, nodes only at the ends,</p>
<div class="formula">f₁ = (1 / 2L) √(T / μ)</div>
<p>The set of allowed frequencies is f₁, 2f₁, 3f₁ … — all integer multiples, which is why a stringed
instrument sounds musical.</p>

<p><b>Traps.</b> Using L = λ for the first harmonic (it is L = λ/2); confusing harmonics (n = 1, 2, 3…)
with the number of loops or nodes; and forgetting that the wave speed v depends on tension and mass per
unit length, not on the frequency you drive it at.</p>`,
    used: "Q9: two wires of the same length and material under the same force; f₁ ∝ 1/√(μ) and μ ∝ A.",
    q: ["R0-09"]
  },
  {
    id: "lineardensity", m: "F", stage: 3, t: "Mass per unit length and wave speed on a string",
    pre: ["standingwaves", "waves"],
    one: "The speed of a wave on a string is √(T/μ): tighter is faster, heavier is slower — and μ = ρA links it to the wire's thickness.",
    body: `<p>Two things control how fast a transverse wave runs along a string: the tension T pulling it straight,
and the mass per unit length μ (linear density) resisting being accelerated sideways:</p>
<div class="formula">v = √(T / μ)        μ = mass / length, unit kg m⁻¹</div>
<p>Check the extremes: more tension → faster (a tight guitar string sounds higher); more mass per metre →
slower (the bass strings on a piano are thick and heavy). Dimensional check: T is a force, kg m s⁻²; μ is
kg m⁻¹; T/μ has units m² s⁻², so √(T/μ) is a speed ✓.</p>

<p><b>Connecting μ to the wire's size.</b> A wire of cross-sectional area A made of a material of density ρ
has mass per unit length:</p>
<div class="formula">μ = ρ A          (mass of one metre = density × volume of one metre = ρ × A × 1)</div>
<p>Put the two together with the resistance formula R = ρ<sub>e</sub>L/A and the dependencies start to
compete, which is exactly what Q9 is built on: a fatter wire is heavier (lower frequency) but has lower
resistance.</p>

<p><b>Traps.</b> Using the wire's total mass instead of mass per unit length; and using the electrical
resistivity symbol ρ for density — they share a letter, so always check which one the question means.</p>`,
    used: "Q9: μ = ρA means a thicker wire lowers f₁, while R = ρL/A means it also lowers R; the ratio of the two effects gives 4:1.",
    q: ["R0-09"]
  },
  {
    id: "refractive", m: "G", stage: 3, t: "Refractive index and the normal",
    pre: ["waves"],
    one: "The refractive index n says how much a medium slows light down; angles in optics are measured from the normal, the line at 90° to the surface.",
    body: `<p>Light travels at c = 3.0 × 10⁸ m s⁻¹ in a vacuum and more slowly in matter. The <b>refractive
index</b> of a medium is the ratio:</p>
<div class="formula">n = c / v        (≥ 1;  air ≈ 1.00,  water ≈ 1.33,  glass ≈ 1.5)</div>
<p>The bigger n is, the more the medium slows light, and the more sharply a ray bends on entering it.</p>

<p><b>The normal.</b> Every refraction question uses angles measured from the <b>normal</b> — an imaginary
line drawn at 90° to the surface at the point where the ray hits. This convention is not optional: the
sine rule of refraction only works with angles from the normal. If a diagram gives you the angle from the
surface, convert it: (angle from normal) = 90° − (angle from surface).</p>

<p><b>Which way does it bend?</b> Entering a <i>denser</i> medium (higher n) the ray slows and bends
<b>towards</b> the normal, so the angle to the normal gets smaller. Entering a less dense medium it bends
<b>away</b> from the normal. That single rule is a check on every answer.</p>

<p><b>Traps.</b> Measuring from the surface by mistake — the single most common optics error, and the
entire point of Q2. And thinking n is a property of the ray rather than of the medium.</p>`,
    used: "Q2 asks for Snell's law with angles drawn from the surface; the whole question is this convention.",
    q: ["R0-02"]
  },
  {
    id: "snell", m: "G", stage: 3, t: "Snell's law",
    pre: ["refractive", "waves"],
    one: "n₁ sin θ₁ = n₂ sin θ₂ — the product of refractive index and the sine of the angle to the normal is conserved across a boundary.",
    body: `<p>When a ray crosses from medium 1 into medium 2, the angles to the normal and the refractive indices are
linked by:</p>
<div class="formula">n₁ sin θ₁ = n₂ sin θ₂        so   sin θ₁ / sin θ₂ = n₂ / n₁</div>
<p><b>Where it comes from.</b> The frequency must be the same on both sides (it is set by the source), but
the speed changes from c/n₁ to c/n₂, so the wavelength changes in the same ratio. Matching the wavefronts
up along the boundary — the same number of crests must arrive on each side in the same time — gives
sin θ₁ / sin θ₂ = v₁ / v₂ = n₂ / n₁, which rearranges to the law above.</p>

<p><b>Sanity checks that catch sign and inversion errors:</b></p>
<ul>
<li>Going into a denser medium (n₂ &gt; n₁) means sin θ₂ &lt; sin θ₁, so the ray bends towards the normal ✓.</li>
<li>Hitting the boundary head-on (θ₁ = 0) gives θ₂ = 0: no bending along the normal ✓.</li>
<li>Swapping the two media must invert the ratio ✓.</li>
</ul>

<p><b>Worked (Q16).</b> Air to glass, n₂ = √2, angle of incidence 30°:</p>
<div class="formula">1 · sin 30° = √2 · sin θ₂
sin θ₂ = 0.5 / 1.414 = 0.354       θ₂ ≈ 20.7°</div>
<p>The ray bends towards the normal, as expected going into glass.</p>

<p><b>Traps.</b> Writing n₁ sin θ₂ = n₂ sin θ₁ (inverted — check it by asking which medium gives the
smaller angle); and using degrees/radians inconsistently when evaluating trig functions.</p>`,
    used: "Q2 (with angles from the surface) and Q16 (finding the refracted angle in a glass cube).",
    q: ["R0-02", "R0-16"]
  },
  {
    id: "lightspeed", m: "G", stage: 3, t: "Speed of light in a medium",
    pre: ["refractive", "snell"],
    one: "v = c/n is the definition of refractive index, so the time to cross a thickness d at an angle is d' / v where d' is the actual path length.",
    body: `<p>From the definition n = c/v, the speed in a medium of index n is:</p>
<div class="formula">v = c / n        (glass with n = 1.5: 2 × 10⁸ m s⁻¹, two thirds of c)</div>
<p><b>Time of flight.</b> Speed is constant inside a uniform medium, so time = distance/speed. But the
distance is the <i>path length travelled</i>, not the straight-through thickness — if the ray crosses at
an angle, it travels further.</p>

<p><b>Example (Q16).</b> A cube of side a, light entering at 30° to the normal, and we want the time to
reach the opposite face. From Snell's law the refracted angle θ₂ satisfies sin θ₂ = sin 30°/√2, so
cos θ₂ = √(1 − ⅛) = √(7/8). The ray travels along the hypotenuse, so the distance is a/cos θ₂ and:</p>
<div class="formula">path length = a / cos θ₂ = a / √(7/8) = a√(8/7)
speed       = c / n = c/√2
time        = a√(8/7) / (c/√2) = a·√(8/7)·√2 / c = a√(16/7) / c = 4a / (√7 c)</div>
<p>Note it is longer than a/c, as it must be: the ray goes slower than c and also travels further than a.</p>

<p><b>Traps.</b> Using the thickness a as the path length (that only works for a ray at 90° incidence);
and dividing by c instead of by c/n.</p>`,
    used: "Q16: the time is (4/√7)·a/c — longer than a/c for two separate reasons.",
    q: ["R0-16"]
  },
  {
    id: "inversesquare", m: "F", stage: 3, t: "Point sources and the inverse-square law",
    pre: [],
    one: "Light from a point source spreads over the surface of an expanding sphere, so the intensity falls as 1/d² — halve the distance and you get four times the intensity.",
    body: `<p>A point source radiating power P fills an ever-growing sphere. At distance d the power is spread over
the sphere's area 4πd², so the <b>intensity</b> (power per unit area) is:</p>
<div class="formula">I = P / (4π d²)          unit: W m⁻²</div>
<p>The light is not lost — it is spread thinner. Double the distance and the same power covers four times
the area, so the intensity is a quarter. Halve the distance and it is four times brighter:</p>
<div class="formula">I₂ / I₁ = (d₁ / d₂)²</div>
<p>This is why moving a lamp twice as close does not make a photo twice as bright but four times, and why
the inverse-square law appears in gravity, electrostatics and sound as well — anywhere something spreads
out evenly in three dimensions.</p>

<p><b>In Q23</b> the distance to the point source is halved, so the intensity rises by a factor of 4, and
the LDR's resistance then follows from the power law read off the log–log graph.</p>

<p><b>Traps.</b> Using 1/d instead of 1/d²; and applying the law to an extended or focused source, where it
does not hold.</p>`,
    used: "Q23: halving the distance multiplies the intensity by 4 before the LDR's power law is applied.",
    q: ["R0-23"]
  },
  {
    id: "charge", m: "H", stage: 4, t: "Charge, current, potential difference and emf",
    pre: [],
    one: "Current is the rate of flow of charge; potential difference is energy per coulomb given up; emf is energy per coulomb supplied.",
    body: `<p><b>Charge</b> Q is a property of matter, measured in coulombs (C). The electron carries
−1.6 × 10⁻¹⁹ C, so ordinary charges are enormous numbers of electrons.</p>

<p><b>Current</b> is the rate at which charge flows past a point:</p>
<div class="formula">I = ΔQ / Δt        unit: ampere (A) = C s⁻¹</div>
<p>By convention current flows from positive to negative round the outside of a circuit — the opposite
way to the electrons, a historical accident that never causes trouble so long as you are consistent.</p>

<p><b>Potential difference</b> (pd, “voltage”) between two points is the energy transferred per unit charge
moving between them:</p>
<div class="formula">V = W / Q        unit: volt (V) = J C⁻¹</div>
<p>So a 6 V cell gives 6 J of energy to every coulomb that passes through it, and a component with 6 V
across it takes 6 J from every coulomb.</p>

<p><b>emf</b> (electromotive force) is not a force at all, despite the name — it is the energy per coulomb
the <i>source</i> supplies, converting chemical (or mechanical) energy into electrical energy. It is also
measured in volts. A real cell has internal resistance r, so the pd across its terminals is ε − Ir; when
the internal resistance is negligible (as in Q8) the terminal pd equals ε exactly.</p>

<p><b>Traps.</b> Thinking emf and pd are the same thing (emf is supplied, pd is delivered — they are equal
only with no internal resistance and no current); and thinking current gets “used up” round a circuit —
the same current returns to the cell, it just has less energy per coulomb.</p>`,
    used: "Q8: a cell of emf ε with negligible internal resistance, so the loop is driven by ε.",
    q: ["R0-08"]
  },
  {
    id: "resistance", m: "H", stage: 4, t: "Resistance and Ohm's law",
    pre: ["charge"],
    one: "Resistance measures how hard it is to push current through something: R = V/I, and for an ohmic conductor it is a constant.",
    body: `<p>The <b>resistance</b> of a component is the ratio of the pd across it to the current through it:</p>
<div class="formula">R = V / I        unit: ohm (Ω) = V A⁻¹</div>
<p>Physically, electrons drifting through a metal collide with the vibrating ions and lose energy; that
opposition is resistance.</p>

<p><b>Ohm's law</b> is a statement about certain materials, not a definition: for a metal at constant
temperature, V is proportional to I, so R is a constant and the V–I graph is a straight line through the
origin. A filament lamp is <i>not</i> ohmic — as it gets hotter its resistance rises and the graph curves.</p>

<p><b>Power</b> dissipated in a resistor combines P = VI with V = IR:</p>
<div class="formula">P = VI = I²R = V²/R        unit: watt (W)</div>

<p><b>Two ways of thinking about it.</b> Electrically, R = V/I. Mechanically, a longer or thinner conductor
has more resistance — which is what the resistivity formula makes precise.</p>

<p><b>Traps.</b> Assuming R is constant for every component (lamps and diodes are not ohmic); and quoting
Ohm's law as V = IR as if it were always true — it is the <i>definition</i> of R in general, but the
statement “R does not depend on V” is the actual law.</p>`,
    used: "Q6, Q8 and Q25 all need resistances combined; Q23 is about a resistance that changes with light.",
    q: ["R0-06"]
  },
  {
    id: "resistivity", m: "H", stage: 4, t: "Resistivity: R = ρL/A",
    pre: ["resistance"],
    one: "Resistance doubles with length and halves with cross-sectional area; resistivity ρ is the material constant that ties them together.",
    body: `<p>The resistance of a uniform wire depends on its shape as well as its material:</p>
<div class="formula">R = ρ L / A        ρ = resistivity (Ω m),  L = length,  A = cross-sectional area</div>
<p><b>Longer</b> wire → more collisions along the way → bigger R (proportional to L). <b>Fatter</b> wire →
more parallel routes for the current → smaller R (inversely proportional to A), just like a wider pipe
carries water more easily. Copper is about 1.7 × 10⁻⁸ Ω m; nichrome about 100 times more.</p>

<p><b>The competing effect that Q9 is built on.</b> For wires of the same material and length, making a
wire fatter changes two things at once:</p>
<div class="formula">A ↑   →   mass per unit length  μ = ρ_density · A   ↑     →  wave speed √(T/μ) ↓
A ↑   →   electrical resistance R = ρ_elec · L / A   ↓</div>
<p>Two different ρ symbols appear here — density and resistivity — which is a genuine trap in this
question. Read each symbol in context.</p>

<p><b>Traps.</b> Using diameter instead of area (A = πr², so doubling the diameter quarters the
resistance); and confusing the two ρ's.</p>`,
    used: "Q9: the frequency ratio 2:1 forces an area ratio of 1:4, which forces a resistance ratio of 4:1.",
    q: ["R0-09"]
  },
  {
    id: "seriesparallel", m: "H", stage: 4, t: "Resistors in series and in parallel",
    pre: ["resistance"],
    one: "Series shares one current and adds the resistances; parallel shares one voltage and adds the reciprocals.",
    body: `<p><b>Series</b> — components in one chain. The same current flows through each, and the voltages add:</p>
<div class="formula">R_total = R₁ + R₂ + …
V_total = V₁ + V₂ + …        I is the same everywhere</div>
<p><b>Parallel</b> — components side by side between the same two nodes. Each has the same pd across it, and
the currents add:</p>
<div class="formula">1/R_total = 1/R₁ + 1/R₂ + …        V is the same across each branch
for two:  R_total = R₁R₂ / (R₁ + R₂)      ("product over sum")</div>
<p>Where the reciprocal rule comes from: in parallel the currents add, I = I₁ + I₂, and each branch has the
same V, so V/R = V/R₁ + V/R₂ — divide through by V and you have it.</p>

<p><b>Two checks you should always run:</b></p>
<ul>
<li>A parallel combination is always <b>less than the smallest</b> branch — more routes means less
opposition. If you get a bigger number you have used the series formula.</li>
<li>Two equal resistors R in parallel give exactly R/2; three equal give R/3; n equal give R/n.</li>
</ul>
<p><b>Example (Q6).</b> Three 3 Ω resistors in parallel give 3/3 = 1 Ω; put that in series with one more
3 Ω and you have 4 Ω for 4 × £1 = £4.</p>

<p><b>Traps.</b> Adding parallel resistances directly; and forgetting that the “product over sum” shortcut
only works for <i>two</i> branches.</p>`,
    used: "Q6 (three 3 Ω in parallel = 1 Ω, then +3 Ω in series) and Q25 (a 3×3 grid of twelve).",
    q: ["R0-06", "R0-25"]
  },
  {
    id: "networks", m: "H", stage: 4, t: "Reducing a network: symmetry, folds and bridges",
    pre: ["seriesparallel"],
    one: "Most 'impossible' networks collapse if you spot two nodes at the same potential — anything joining them carries no current and can simply be deleted.",
    body: `<p>Series and parallel rules alone cannot reduce every network — Q25's 3 × 3 grid of twelve identical
resistors is the classic example. The extra weapon is <b>symmetry</b>.</p>

<p><b>Rule 1 — equal potentials carry nothing.</b> If two nodes must be at the same potential, a resistor
joining them has no pd across it, so no current flows through it, so it can be removed (or shorted —
either way nothing changes).</p>

<p><b>Rule 2 — the balanced Wheatstone bridge.</b> If R₁/R₂ = R₃/R₄ in a bridge, the middle resistor carries
no current:</p>
<div class="formula">R₁   R₂
  ╲ ╱
   ╳        no current in the middle arm when R₁/R₂ = R₃/R₄
   ╱ ╲
R₃   R₄</div>

<p><b>Finding the equal potentials.</b> Two symmetry arguments do the work:</p>
<ul>
<li><b>Mirror symmetry.</b> If the network and the terminals are both unchanged by a reflection, mirrored
nodes sit at equal potentials — so you can fold the network in half.</li>
<li><b>Antisymmetry.</b> If a reflection <i>swaps</i> the terminals, then (choosing the zero of potential
halfway between) mirrored nodes sit at equal and opposite potentials, V and −V. A node that the
reflection leaves <i>fixed</i> therefore has V = −V, so V = 0.</li>
</ul>

<p><b>Example (Q25), the pair QT.</b> Current enters at Q on the top edge and leaves at T on the bottom
edge. Left–right reflection leaves both terminals where they are, so columns mirror and can be folded.
Top–bottom reflection <i>swaps</i> Q and T, so the nodes it leaves fixed — the centre and the two middle-row
side nodes — are all at 0 V. The two resistors joining the centre to those side nodes therefore carry no
current: delete them. What is left reduces by series and parallel to ½ Ω from Q to the zero node and ½ Ω
from the zero node to T, giving:</p>
<div class="formula">R(QT) = ½ + ½ = 1 Ω        (exactly, no algebra)</div>
<p>The same treatment on the other four pairs gives QS = 7/12, PS = 7/8, PR = 5/4, PU = 3/2 — five values,
of which 1 Ω is the median.</p>

<p><b>Traps.</b> Assuming every grid reduces by series/parallel alone (this one does not); and merging nodes
that are not connected — you may short together nodes at equal potential, but you must not merge nodes
merely because they look symmetric in a drawing where the terminals break the symmetry.</p>`,
    used: "Q25: the median of PR, PS, PU, QS, QT is QT = 1 Ω, found by symmetry rather than by solving nine nodal equations.",
    q: ["R0-25"]
  },
  {
    id: "meters", m: "H", stage: 4, t: "Ideal ammeters and voltmeters",
    pre: ["charge", "seriesparallel"],
    one: "An ideal ammeter is a wire (0 Ω) and an ideal voltmeter is a gap (∞ Ω) — which means each one silently deletes part of the circuit.",
    body: `<p><b>Ammeters</b> measure the current through themselves, so they go <b>in series</b> with the component.
To avoid changing what they measure, an ideal ammeter has <b>zero resistance</b> — it is just a piece of
wire. Consequence: anything connected in parallel with an ideal ammeter is <b>short-circuited</b> and
carries no current.</p>

<p><b>Voltmeters</b> measure the pd between two points, so they go <b>in parallel</b> with the component. To
avoid drawing current away, an ideal voltmeter has <b>infinite resistance</b> — it is a gap. Consequence:
anything in series with an ideal voltmeter carries no current, so that branch is dead.</p>

<p><b>The move that solves Q8.</b> Look at the diagram and delete what the meters kill. The ammeter is in
parallel with one of the three resistors, so that resistor is shorted out of existence. The voltmeter sits
in a lead that feeds a junction, so that lead carries nothing. What survives is one simple loop: the cell
and two resistors of R in series.</p>
<div class="formula">loop resistance = R + R = 2R
current         I = ε / (2R)        ← the ammeter reading
pd across one R   = I R = ε/2        ← the voltmeter reading</div>
<p>The whole question is the two deletions; the arithmetic afterwards is one line.</p>

<p><b>Traps.</b> Treating the meters as extra resistors to add in; and misreading where each meter is
connected — a meter drawn in parallel with a resistor kills it, whereas one drawn in series merely stops
that branch.</p>`,
    used: "Q8: two of the five components do nothing once you apply what 'ideal' means — the answer is ε/2 and ε/(2R).",
    q: ["R0-08"]
  },
  {
    id: "kirchhoff", m: "H", stage: 4, t: "Kirchhoff's two laws",
    pre: ["charge", "resistance"],
    one: "Charge cannot pile up at a junction and energy cannot appear from nowhere: those two facts give the two circuit laws.",
    body: `<p><b>Kirchhoff's first law</b> (current law) — at any junction:</p>
<div class="formula">Σ I in = Σ I out</div>
<p>Because charge is conserved and cannot accumulate at a point. A node with three wires, two bringing
3 A and 2 A, must send 5 A down the third.</p>

<p><b>Kirchhoff's second law</b> (voltage law) — round any closed loop:</p>
<div class="formula">Σ emf = Σ (IR)        (sum of the rises = sum of the drops)</div>
<p>Because energy is conserved: a coulomb that gains energy in the cell must lose exactly as much going
round the rest of the loop and come back to the same potential.</p>

<p><b>How to use them.</b> Label a current in every branch (direction is arbitrary — a negative answer just
means it flows the other way), write the current law at each junction, write the voltage law round each
independent loop, and solve. Two rules of thumb: the current law at a junction of two branches says the
currents are equal, and components in parallel always share the same pd.</p>

<p><b>Traps.</b> Getting the sign wrong when going round a loop — fix a direction and treat every drop
against that direction as positive, every rise as negative, consistently. And writing more loop equations
than there are independent loops.</p>`,
    used: "Q8 can be done with Kirchhoff's laws, though spotting what the ideal meters delete is faster.",
    q: ["R0-08"]
  },
  {
    id: "divider", m: "H", stage: 4, t: "The potential divider",
    pre: ["seriesparallel", "resistance"],
    one: "Two resistors in series split the supply voltage in proportion to their resistances — the basis of every sensor circuit.",
    body: `<p>Put two resistors R₁ and R₂ in series across a supply V. The same current I = V/(R₁ + R₂) flows through
both, so the pd across R₂ is:</p>
<div class="formula">V_out = I R₂ = V · R₂ / (R₁ + R₂)</div>
<p>So the bigger resistor takes the bigger share of the voltage. Three quick consequences: if the two are
equal the output is half the supply; if R₂ is tiny the output is nearly 0; if R₂ is huge the output is
nearly the whole supply.</p>

<p><b>Why it matters.</b> Replace one resistor with a sensor whose resistance changes — an LDR (light), a
thermistor (temperature), a strain gauge — and the output voltage becomes a measurable signal that tracks
the physical quantity. That is how almost every analogue sensor is read.</p>

<p>In Q23 the LDR is the sensing element: its resistance falls as the light gets brighter, so the voltage
across it reports the light level.</p>

<p><b>Traps.</b> Using the total resistance instead of just R₂ in the numerator; and forgetting that the
output is measured across one resistor, not across the pair.</p>`,
    used: "Q23: an LDR whose resistance depends on light is the classic divider sensor.",
    q: ["R0-23"]
  },
  {
    id: "capacitors", m: "I", stage: 4, t: "Capacitance and Q = CV",
    pre: ["charge"],
    one: "A capacitor stores charge: the charge stored is proportional to the pd across it, and the constant is the capacitance.",
    body: `<p>A <b>capacitor</b> is two conducting plates separated by an insulator. Push charge onto one plate and an
equal amount is pushed off the other, so equal and opposite charges ±Q sit on the plates and energy is
stored in the electric field between them.</p>
<div class="formula">C = Q / V        unit: farad (F) = C V⁻¹
Q = C V</div>
<p>A 100 μF capacitor at 6 V holds Q = 100 × 10⁻⁶ × 6 = 6 × 10⁻⁴ C. The farad is a huge unit, so real
capacitors are quoted in μF, nF or pF.</p>

<p><b>How many electrons is that?</b> Divide by the elementary charge e = 1.6 × 10⁻¹⁹ C. This is the bridge
that Q17 uses: a capacitor charged to a potential V holds N = Q/e = CV/e electrons' worth of charge.</p>

<p><b>Energy stored.</b> The pd rises from 0 to V as charge builds, so the average pd during charging is
V/2 and:</p>
<div class="formula">E = ½ Q V = ½ C V²</div>
<p>For a parallel-plate capacitor, C = ε₀ε_r A/d — bigger plates and a smaller gap give more capacitance.</p>

<p><b>Traps.</b> Saying “a capacitor stores charge” loosely — the <i>net</i> charge is zero, it stores equal
and opposite charges on the two plates. And confusing C (capacitance, a fixed property) with Q (charge,
which changes).</p>`,
    used: "Q17: the number of photoelectrons needed is Q/e, with Q = CV and V the stopping potential.",
    q: ["R0-17"]
  },
  {
    id: "loggraphs", m: "H", stage: 4, t: "Log–log graphs: the gradient is the power",
    pre: ["stdform"],
    one: "If y = kxⁿ, plotting log y against log x gives a straight line whose gradient is n — a way to read a power law straight off a graph.",
    body: `<p>Take logarithms of both sides of y = k xⁿ:</p>
<div class="formula">y = k xⁿ
log y = log k + n log x</div>
<p>That is the equation of a straight line in the variables (log x, log y): intercept log k and
<b>gradient n</b>. So a log–log plot turns any power law into a straight line whose slope is the
exponent — which is how you discover a power law from data.</p>

<p><b>Example (Q23).</b> The graph shows log(R/Ω) against log(I/W m⁻²), passing through (0, 5.2) and
(4, 1.8):</p>
<div class="formula">gradient = (1.8 − 5.2) / (4 − 0) = −3.4 / 4 = −0.85
so   R ∝ I<sup>−0.85</sup></div>
<p>The negative gradient says what you would expect of an LDR: brighter light, lower resistance.</p>

<p><b>Using it without a calculator.</b> Halving the distance to a point source makes the intensity 4 times
larger, so:</p>
<div class="formula">R/R₀ = 4<sup>−0.85</sup>
4<sup>3/4</sup> = 2<sup>3/2</sup> = 2√2 ≈ 2.83,  and 4<sup>0.1</sup> ≈ 1.15
so  4<sup>0.85</sup> ≈ 2.83 × 1.15 ≈ 3.3   →   R/R₀ ≈ 1/3.3 ≈ 0.3</div>
<p>Since the options are 0.1, 0.3, 0.5, 0.7, 0.9, the answer is 0.3 R₀.</p>

<p><b>Traps.</b> Reading the gradient as the gradient of the original curve (it is not); getting the axes
the wrong way round (that gives you 1/n); and using log₁₀ versus ln inconsistently — the <i>gradient</i>
is the same either way, but only if both axes use the same base.</p>`,
    used: "Q23: gradient −0.85, intensity × 4, so R ≈ 0.3 R₀.",
    q: ["R0-23"]
  },
  {
    id: "photons", m: "L", stage: 5, t: "Photons: light arrives in packets of energy hf",
    pre: [],
    one: "Light is quantised: its energy comes in packets of hf, so a higher frequency means more energy per packet, not more light.",
    body: `<p>A <b>photon</b> is one packet of electromagnetic energy. Its energy is fixed by the frequency alone:</p>
<div class="formula">E = h f = h c / λ        h = 6.63 × 10⁻³⁴ J s  (Planck's constant)</div>
<p>The constant h is tiny, which is why we do not notice the graininess of light in everyday life: a
40 W lamp emits roughly 10²⁰ photons every second.</p>

<p><b>Two consequences worth internalising:</b></p>
<ul>
<li><b>Higher frequency, more energy per photon.</b> Blue light (short λ, high f) carries more energy per
photon than red. That is why ultraviolet can cause sunburn and radio waves cannot.</li>
<li><b>Brighter means more photons, not more energetic photons.</b> Turning up the intensity at a fixed
frequency delivers more packets of the same size.</li>
</ul>
<p>That second point is the heart of the photoelectric effect, and it is what classical wave physics got
wrong.</p>

<p><b>Units check.</b> h has units J s; multiplied by f (s⁻¹) it gives J ✓. Alternatively h is an
<i>action</i> — energy × time — which is also angular momentum, and its dimensions are M L² T⁻¹.</p>

<p><b>Traps.</b> Using λ in the wrong place — E = hc/λ means <i>longer</i> wavelength means <i>less</i>
energy. And confusing “intensity” (number of photons) with “frequency” (energy per photon).</p>`,
    used: "Q11 (photons from transitions between energy levels) and Q17 (photoelectrons).",
    q: ["R0-11"]
  },
  {
    id: "levels", m: "L", stage: 5, t: "Atomic energy levels and transitions",
    pre: ["photons"],
    one: "Electrons in an atom can only occupy certain energies; jumping between two levels emits or absorbs a photon whose energy is exactly the gap.",
    body: `<p>Quantum mechanics restricts an atom's electrons to a set of discrete <b>energy levels</b> — rungs on a
ladder, not a ramp. An electron cannot sit between them. Level n = 1 is the <b>ground state</b> (lowest);
higher n are excited states; n = ∞ means the electron has escaped (ionisation).</p>

<p>When an electron drops from a higher level E₂ to a lower one E₁, the lost energy leaves as a single
photon:</p>
<div class="formula">h f = E₂ − E₁        ( emission:  downwards )
h f = E₁ − E₂ ...    ( absorption: upwards, photon of exactly this energy is needed )</div>
<p>So each transition produces light of one precise frequency — which is why atoms give <b>line spectra</b>
rather than a continuous rainbow, and why each element has its own fingerprint of lines.</p>

<p><b>Counting transitions (Q11).</b> With ten levels and every transition allowed, each photon energy
corresponds to a <i>pair</i> of levels, so there are C(10,2) = 45 possible photon energies. Note that
“maximum” is doing work in the question: two different pairs could accidentally have the same gap, which
would reduce the count — 45 is the upper bound.</p>

<p><b>Traps.</b> Counting 90 (counting each transition twice, once per direction); counting 100 (including
“transitions” from a level to itself, which emit nothing); and thinking all 45 photons have different
frequencies guaranteed rather than at most.</p>`,
    used: "Q11: ten levels, all transitions allowed → at most C(10,2) = 45 photon energies.",
    q: ["R0-11"]
  },
  {
    id: "photoelectric", m: "L", stage: 5, t: "The photoelectric effect",
    pre: ["photons"],
    one: "Shine light on a metal and electrons come out — but only above a threshold frequency, and instantly, which wave theory cannot explain.",
    body: `<p><b>The observations.</b> Light above a certain <b>threshold frequency</b> f₀ knocks electrons out of a
metal surface. Below f₀ nothing happens, no matter how bright the light or how long you wait. Above f₀ the
electrons appear immediately.</p>

<p><b>Why this killed the wave picture.</b> If light were a continuous wave, energy would arrive steadily
and even dim low-frequency light should eventually build up enough to eject an electron. It never does.
The explanation is that light arrives in photons of energy hf, and one photon is absorbed by one
electron:</p>
<div class="formula">h f = φ + KE_max        (energy conservation for one photon, one electron)</div>
<p>Here <b>φ</b> (phi) is the <b>work function</b>, the energy needed just to get an electron out of the
surface; φ = h f₀. Whatever is left over becomes the electron's kinetic energy:</p>
<div class="formula">KE_max = h f − φ = h (f − f₀)</div>
<p><b>Reading the equation:</b></p>
<ul>
<li>Below threshold (hf &lt; φ): no emission at all, however intense the light.</li>
<li>Increasing the <b>frequency</b> raises KE_max — the electrons come out faster.</li>
<li>Increasing the <b>intensity</b> raises the <i>number</i> emitted, not their maximum energy — more
photons, same energy each.</li>
</ul>

<p><b>Traps.</b> Thinking brighter light gives faster electrons (it gives more electrons); and using the
photon energy as the electron's kinetic energy without subtracting the work function.</p>`,
    used: "Q17: photoelectrons emitted from a capacitor plate, with KE_max = h(f − f₀).",
    q: ["R0-17"]
  },
  {
    id: "stopping", m: "L", stage: 5, t: "Stopping potential",
    pre: ["photoelectric", "charge"],
    one: "Apply a reverse voltage until even the fastest photoelectron cannot reach the other side — that voltage measures the electrons' maximum kinetic energy.",
    body: `<p>In a photoelectric experiment, make the collector negative relative to the emitting plate. Electrons
now have to climb an electric potential hill. Slow ones fall back; increase the reverse voltage until even
the <i>fastest</i> fail to arrive and the current falls to zero. That voltage is the <b>stopping
potential</b> V_s.</p>

<p>An electron of charge e climbing through a potential difference V gains potential energy eV, so the
condition “just stopped” is that its kinetic energy is exactly used up:</p>
<div class="formula">e V_s = KE_max = h f − φ = h (f − f₀)
V_s = h (f − f₀) / e</div>
<p>Two things to notice. The stopping potential depends on <b>frequency only</b>, not on intensity — a
direct experimental confirmation of the photon picture. And a plot of V_s against f is a straight line of
gradient h/e, which is how h is measured.</p>

<p><b>Example (Q17).</b> Light of frequency f hits a capacitor plate of threshold f₀. The emitted electrons
charge the capacitor until its pd reaches the stopping potential, at which point further electrons can no
longer cross. So the final pd is V_s = h(f − f₀)/e, the charge stored is Q = CV_s, and the number of
electrons that crossed is:</p>
<div class="formula">N = Q / e = C V_s / e = C h (f − f₀) / e²</div>
<p>The e² in the answer is not a typo — one e converts energy to a voltage, the other converts charge to a
number of electrons. That is exactly the kind of thing a units check confirms: C × J C⁻¹ / C is a pure
number ✓.</p>

<p><b>Traps.</b> Forgetting the second division by e when the question asks for a <i>number</i> of
electrons; and using KE_max where the question needs the voltage.</p>`,
    used: "Q17: the number of photoelectrons is Ch(f − f₀)/e² — two factors of e, for two different reasons.",
    q: ["R0-17"]
  },
  {
    id: "nuclide", m: "K", stage: 5, t: "Nuclide notation and the nuclear equation",
    pre: [],
    one: "ᵃ_Z X: the top number counts nucleons, the bottom counts protons; both are conserved in every nuclear reaction.",
    body: `<p>A nuclide is written <b>ᴬ_Z X</b>, where X is the element symbol:</p>
<div class="formula">A  mass number  = protons + neutrons     (roughly the mass, in u)
Z  proton number = atomic number          (this defines the element)
neutrons = A − Z</div>
<p>So ²³⁸₉₂U has 92 protons and 146 neutrons. The chemical identity is set by Z alone; changing Z makes a
different element, changing only the neutron count makes an <b>isotope</b> of the same element.</p>

<p><b>Two conservation rules govern every nuclear equation:</b></p>
<div class="formula">total A is conserved      (nucleons are neither created nor destroyed)
total Z is conserved      (charge is conserved)</div>
<p>Strictly, mass–energy is conserved and a little mass becomes energy — but the <i>count</i> of nucleons is
conserved exactly, which is the bookkeeping you need.</p>

<p><b>Traps.</b> Mixing up which number is which (top = mass, bottom = charge); and thinking the element
symbol follows A rather than Z.</p>`,
    used: "Q4: tracking ²³⁸₉₂U down to ²⁰⁶₈₂Pb is pure A/Z bookkeeping.",
    q: ["R0-04"]
  },
  {
    id: "alphabeta", m: "K", stage: 5, t: "Alpha and beta-minus decay",
    pre: ["nuclide"],
    one: "Alpha removes 4 from A and 2 from Z; beta-minus leaves A alone and adds 1 to Z — a neutron turning into a proton.",
    body: `<p><b>Alpha decay.</b> The nucleus ejects a helium nucleus, ⁴₂He — two protons and two neutrons bound
together:</p>
<div class="formula">ᴬ_Z X  →  ᴬ⁻⁴_{Z−2} Y  +  ⁴₂He
A decreases by 4,   Z decreases by 2</div>
<p>Alpha particles are heavy, doubly charged and highly ionising, so they are stopped by a sheet of paper
or a few centimetres of air.</p>

<p><b>Beta-minus decay.</b> A neutron in the nucleus turns into a proton, emitting an electron (the
β⁻ particle) and an antineutrino:</p>
<div class="formula">n  →  p  +  e⁻  +  ν̄
ᴬ_Z X  →  ᴬ_{Z+1} Y  +  e⁻  +  ν̄
A unchanged,   Z increases by 1</div>
<p>Note the mass number does not change: a neutron has become a proton, so the nucleon count is the same
but the charge has gone up by one. The electron is created in the process — it was not orbiting inside the
nucleus beforehand.</p>

<p>(Beta-<i>plus</i> is the mirror image: a proton becomes a neutron, emitting a positron, so Z goes down
by 1. Gamma emission changes neither A nor Z — it just releases surplus energy.)</p>

<p><b>Traps.</b> Changing A in a beta decay; and getting the sign of the Z change wrong — β⁻ makes the
nucleus more positive, because a negative electron carrying away negative charge leaves positive charge
behind.</p>`,
    used: "Q4: eight alphas take A from 238 to 206, and six beta-minus decays repair Z from 76 back to 82.",
    q: ["R0-04"]
  },
  {
    id: "decayseries", m: "K", stage: 5, t: "Decay series: working out how many of each",
    pre: ["alphabeta", "nuclide"],
    one: "The mass number only changes with alpha decays, so count those first — then let the proton number tell you how many betas are needed.",
    body: `<p>When a heavy nucleus decays through a chain of alphas and beta-minus decays to a known end product, the
two conservation rules give you two equations and there is a fixed order to solve them:</p>
<ul>
<li><b>Step 1 — use A to count the alphas.</b> Only alpha decay changes A (−4 each), so
n<sub>α</sub> = (A<sub>start</sub> − A<sub>end</sub>)/4.</li>
<li><b>Step 2 — work out what the alphas did to Z.</b> Each alpha takes 2 off.</li>
<li><b>Step 3 — use the gap in Z to count the betas.</b> Each β⁻ adds 1.</li>
</ul>
<p><b>Example (Q4).</b> ²³⁸₉₂U → ²⁰⁶₈₂Pb:</p>
<div class="formula">ΔA = 238 − 206 = 32        →  n_α = 32 / 4 = 8 alpha decays
those 8 alphas remove 8 × 2 = 16 from Z
predicted Z after the alphas: 92 − 16 = 76
actual final Z: 82, so Z must rise by 82 − 76 = 6
n_β = 6 beta-minus decays</div>
<p><b>Always check the direction.</b> Alpha decays take Z <i>down</i> too far (76 versus the needed 82), so
the betas have to push it back up — beta-minus, not beta-plus. Had the arithmetic left Z too high you
would have needed β⁺ decays, and the question says β⁻ only, which is itself a consistency check.</p>

<p><b>Traps.</b> Doing the Z bookkeeping before the A bookkeeping (you cannot count betas until you know how
many alphas there were); and forgetting that each alpha changes Z as well as A.</p>`,
    used: "Q4: 8 alphas and 6 beta-minus decays, found in that order.",
    q: ["R0-04"]
  },
  {
    id: "expdecay", m: "K", stage: 5, t: "Exponential decay and half-life",
    pre: ["logs", "stdform"],
    one: "Radioactive decay is random per nucleus but perfectly predictable in bulk: a fixed fraction decays per unit time, giving a constant half-life.",
    body: `<p>Each nucleus decays at random, but with a fixed probability per second. For a large sample that gives a
simple law: in each fixed interval, the same <i>fraction</i> of what remains decays. The number left is
therefore:</p>
<div class="formula">N = N₀ · 2<sup>−t/T</sup> = N₀ · (½)<sup>t/T</sup>
equivalently  N = N₀ e<sup>−λt</sup>     with  λ = ln 2 / T</div>
<p>where T is the <b>half-life</b> — the time for half the sample to go — and λ is the decay constant.</p>

<p><b>Why “half-life” and not “life”.</b> The time to go from N₀ to N₀/2 is the same as from N₀/2 to
N₀/4, and from N₀/4 to N₀/8. Halving takes a fixed time however much you have, so the quantity never
actually reaches zero — it just keeps halving.</p>

<p><b>Finding a time (Q19).</b> “One third has decayed” means two thirds <i>remain</i>, so N/N₀ = 2/3. Take
logs to bring t down out of the exponent:</p>
<div class="formula">2/3 = 2<sup>−t/T</sup>
log₂(2/3) = −t/T
t = T · log₂(3/2)        (using −log(x) = log(1/x))</div>
<p>Sanity check: log₂(3/2) ≈ 0.585, so the time is a bit over half a half-life — sensible, since
one-third decaying is less than half decaying.</p>

<p><b>Traps.</b> Using 1/3 as the fraction remaining when a third has <i>decayed</i> (two thirds remain);
and writing (2/3)T — a common wrong option — which would only be right if decay were linear rather than
exponential.</p>`,
    used: "Q19: t = T·log₂(3/2), and the options test exactly this confusion.",
    q: ["R0-19"]
  },
  {
    id: "casimir", m: "L", stage: 5, t: "The Casimir effect",
    pre: ["dimensions"],
    one: "Two uncharged plates in a vacuum attract each other: the vacuum itself has fluctuating fields, and between the plates only certain fluctuations fit.",
    body: `<p>Quantum field theory says the vacuum is not empty — electromagnetic fields fluctuate even with no
photons present. Put two perfectly conducting plates very close together and only fluctuations whose
half-wavelength fits a whole number of times into the gap can exist between them. Outside, all
fluctuations are allowed. More modes outside than inside means more radiation pressure pushing the plates
together than pushing them apart, so they <b>attract</b>.</p>

<p>The effect is tiny and only measurable at sub-micron separations. The exact result is:</p>
<div class="formula">p = π² ħ c / 240 r⁴        (ħ = h / 2π)</div>
<p>Note the r⁴ in the denominator: halve the gap and the pressure multiplies by 16. That steep dependence
is why the effect is invisible at everyday distances and dominant below a micron.</p>

<p><b>What Q13 actually asks.</b> Not for the formula — for the <i>dependence on r</i>, assuming the
pressure depends only on h, c and r. Dimensional analysis gives p ∝ h c / r⁴, which matches the true
result's dependence on r. Two honest caveats: dimensional analysis cannot tell h from ħ (they differ only
by the dimensionless 2π), and it cannot produce the numerical factor π²/240. It gives the power law and
nothing else — which is all the question wants.</p>`,
    used: "Q13: the Casimir pressure from h, c and r — dimensional analysis gives p ∝ 1/r⁴.",
    q: ["R0-13"]
  }
];

/* question → key points, the forward direction of the same index */
window.BPHO_QKEYS = {
  "R0-01": ["stressstrain", "strainenergy", "units-elim"],
  "R0-02": ["refractive", "snell", "readdiagram"],
  "R0-03": ["units", "momentum", "dimensions"],
  "R0-04": ["nuclide", "alphabeta", "decayseries"],
  "R0-05": ["waves", "superposition", "pathphase", "coherence"],
  "R0-06": ["resistance", "seriesparallel", "search"],
  "R0-07": ["kinematics", "relvel", "readdiagram"],
  "R0-08": ["charge", "meters", "kirchhoff"],
  "R0-09": ["standingwaves", "lineardensity", "resistivity", "ratio"],
  "R0-10": ["stdform", "estimate", "moles"],
  "R0-11": ["photons", "levels", "counting"],
  "R0-12": ["momcons", "ke", "limits"],
  "R0-13": ["dimensions", "casimir"],
  "R0-14": ["com", "toppling", "statics"],
  "R0-15": ["specificheat", "latentheat", "stdform"],
  "R0-16": ["snell", "lightspeed", "surds"],
  "R0-17": ["photoelectric", "stopping", "capacitors"],
  "R0-18": ["projectile", "smallangle", "limits"],
  "R0-19": ["expdecay", "logs"],
  "R0-20": ["force", "hookeslaw", "shm", "graphshape"],
  "R0-21": ["vectors-resolve", "kinematics", "moments", "statics"],
  "R0-22": ["circular", "conical", "ratio"],
  "R0-23": ["loggraphs", "inversesquare", "divider"],
  "R0-24": ["com", "pe-gpe", "smallchange"],
  "R0-25": ["seriesparallel", "networks", "ratio"]
};
