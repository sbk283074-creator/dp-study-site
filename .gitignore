# ---- Not part of the deployed static site ----
# Original full-stack platform (nested git repo, ~13 GB; deployed separately on Netlify)
dp learning/

# Agent / memory internals
.workbuddy-ai/

# OS junk
.DS_Store
Thumbs.db
*.swp
ehthumbs.db

# Stray Node modules
node_modules/

# ---- PYTHON micro-site: deploy only the built site (PYTHON/index.html) ----
# Source + back-end tooling kept local, not pushed to GitHub Pages.
PYTHON/python-mastery/
PYTHON/verify/
PYTHON/verify-venv/
PYTHON/.workbuddy/
PYTHON/.DS_Store

# ---- Question Bank figures: served from external host (Internet Archive), not committed ----
qbank/figures/

# ---- Eng learning ("The World's Wife" Lit Lab) ----
# Its OWN git repo (github.com/sbk283074-creator/the-worlds-wife) and its OWN
# GitHub Pages site (https://sbk283074-creator.github.io/the-worlds-wife/).
# Kept in this folder for convenience, but must never be committed into
# dp-study-site — git would add it as a submodule gitlink and break Pages.
# The main site links out to it instead.
Eng learning/
