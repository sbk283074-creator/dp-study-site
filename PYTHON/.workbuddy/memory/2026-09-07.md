# 2026-09-07

Built **Python Mastery**, a complete Python teaching book + website for a beginner learner.

**Later addition (22:45):** added 9 SVG UI mockups as "what you're building" figure guides for the
three whole-project chapters — TaskForge ch23 (2 terminal screens), StudyHub ch30 (login/dashboard/
review/stats), Neon Dungeon ch36 (title/gameplay/inventory). Figures live in `assets/figures/*.svg`,
referenced in markdown as `![caption](figures/name.svg)`; `build.py` inlines them and strips any
`<style>` inside the SVG so it cannot leak into the page. Also fixed double-escaped `&` in the
right-hand TOC (`html.unescape` on heading text).

- Location: `/Users/lucas.ma/Downloads/PYTHON/python-mastery/`
- Output: `dist/index.html` — one self-contained ~1.9 MB file, 40 chapters, works offline from `file://`.
- Structure: `chapters/*.md` (40 files, ~159k words) → `build.py` (dependency-free md→html converter) → `template.html` + `assets/style.css` + `assets/app.js`.
- Curriculum: Part I Foundations (1-10), II Leveling Up (11-17), III Real-World Python + TaskForge CLI project (18-23), IV Full-Stack web track + StudyHub capstone (24-30), V Game dev track + Neon Dungeon capstone (31-36), VI Appendices (37-39).
- Rebuild with: `cd python-mastery && python3 build.py`
- Site features: chapter routing via hash, progress + exercise checkboxes in localStorage, search, dark mode, copy buttons, lazy syntax highlighting, `←`/`→`/`/` shortcuts.

Notes:
- Chapter markdown conventions are documented in `STYLE.md`; custom callout syntax is `:::kind Title ... :::` (note/tip/warning/danger/pitfall/scenario/solution/try).
- Authoring was parallelised across subagents; several hit a 429 rate limit on the first pass but the files were still written — always verify with `ls`/`wc -w` before assuming a chapter is missing.
- Verified in a real browser via `agent-browser`: load, routing, TOC, highlighting, theme, search, progress all work.
